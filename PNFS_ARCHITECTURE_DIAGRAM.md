# pNFS Architecture Diagrams

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         NFS Clients                             │
│                    (Linux Kernel, libnfs)                       │
│                                                                 │
│  Mount: mount -t nfs -o vers=4.1 mds-server:/ /mnt            │
└───────────────┬─────────────────────────────┬───────────────────┘
                │                             │
                │ Metadata Ops                │ Data Ops
                │ (OPEN, GETATTR,             │ (READ, WRITE,
                │  LAYOUTGET, etc.)           │  COMMIT)
                │                             │
                ▼                             ▼
┌───────────────────────────┐   ┌─────────────────────────────────┐
│   Metadata Server (MDS)   │   │   Data Servers (DS-1..n)        │
│                           │   │                                 │
│  • NFSv4.1 Control Plane  │   │  • NFSv4.1 Data Plane Only      │
│  • Layout Generation      │◄──┤  • Direct SPDK I/O              │
│  • Device Registry        │   │  • Minimal State                │
│  • State Management       │   │  • High Throughput              │
│  • Session Tracking       │   │                                 │
│  • Lock Management        │   │  DS-1    DS-2    DS-3           │
│                           │   │   │       │       │             │
└───────────┬───────────────┘   └───┼───────┼───────┼─────────────┘
            │                       │       │       │
            │ State Persistence     │       │       │
            ▼                       ▼       ▼       ▼
┌───────────────────────┐   ┌─────────────────────────────────────┐
│  State Backend        │   │     SPDK Block Devices              │
│  • etcd (HA)          │   │     (NVMe, NVMe-oF)                 │
│  • ConfigMap (simple) │   │                                     │
│  • Memory (dev)       │   │  nvme0n1  nvme1n1  nvme2n1          │
└───────────────────────┘   └─────────────────────────────────────┘
```

## Protocol Flow: File Access

```
Client                    MDS                         DS-1        DS-2
  |                        |                           |           |
  |──COMPOUND─────────────►|                           |           |
  |  (PUTROOTFH, GETFH)    |                           |           |
  |◄───────────────────────|                           |           |
  |  (root filehandle)     |                           |           |
  |                        |                           |           |
  |──COMPOUND─────────────►|                           |           |
  |  (PUTFH, OPEN)         |                           |           |
  |◄───────────────────────|                           |           |
  |  (stateid)             |                           |           |
  |                        |                           |           |
  |──COMPOUND─────────────►|                           |           |
  |  (PUTFH, LAYOUTGET)    |                           |           |
  |                        |──check device registry───►|           |
  |                        |◄─────────────────────────|           |
  |◄───────────────────────|                           |           |
  |  (layout: offset 0-4MB → DS-1,                     |           |
  |           offset 4MB-8MB → DS-2)                   |           |
  |                        |                           |           |
  |──COMPOUND─────────────►|                           |           |
  |  (GETDEVICEINFO)       |                           |           |
  |◄───────────────────────|                           |           |
  |  (DS-1: 10.244.1.10:2049,                          |           |
  |   DS-2: 10.244.1.11:2049)                          |           |
  |                        |                           |           |
  |──COMPOUND──────────────────────────────────────────►           |
  |  (PUTFH, READ offset=0, count=4MB)                 |           |
  |◄────────────────────────────────────────────────────           |
  |  (data: 4MB)                                       |           |
  |                        |                           |           |
  |──COMPOUND──────────────────────────────────────────────────────►
  |  (PUTFH, READ offset=4MB, count=4MB)               |           |
  |◄────────────────────────────────────────────────────────────────
  |  (data: 4MB)                                       |           |
  |                        |                           |           |
  |──COMPOUND─────────────►|                           |           |
  |  (PUTFH, LAYOUTRETURN) |                           |           |
  |◄───────────────────────|                           |           |
  |  (success)             |                           |           |
  |                        |                           |           |
  |──COMPOUND─────────────►|                           |           |
  |  (PUTFH, CLOSE)        |                           |           |
  |◄───────────────────────|                           |           |
  |  (success)             |                           |           |
```

## Module Structure

```
spdk-csi-driver/src/
│
├── nfs/                         ← EXISTING (unchanged)
│   ├── mod.rs
│   ├── server_v4.rs             NFSv4.2 standalone server
│   ├── rpc.rs                   RPC message handling
│   ├── xdr.rs                   XDR encoding/decoding
│   └── v4/
│       ├── compound.rs          COMPOUND operations
│       ├── dispatcher.rs        Operation dispatcher
│       ├── filehandle.rs        File handle management
│       ├── protocol.rs          NFSv4 constants
│       ├── state/               State management
│       └── operations/          NFS operations
│
├── pnfs/                        ← NEW (additive only)
│   ├── mod.rs                   Module exports
│   ├── config.rs                Configuration parsing ✅
│   │
│   ├── mds/                     Metadata Server
│   │   ├── mod.rs
│   │   ├── server.rs            MDS main loop
│   │   ├── device.rs            Device registry
│   │   ├── layout.rs            Layout generation
│   │   └── operations/          pNFS operations
│   │       ├── layoutget.rs     LAYOUTGET (opcode 50)
│   │       ├── layoutreturn.rs  LAYOUTRETURN (opcode 51)
│   │       ├── layoutcommit.rs  LAYOUTCOMMIT (opcode 52)
│   │       ├── getdeviceinfo.rs GETDEVICEINFO (opcode 47)
│   │       └── getdevicelist.rs GETDEVICELIST (opcode 48)
│   │
│   └── ds/                      Data Server
│       ├── mod.rs
│       ├── server.rs            DS main loop
│       ├── io.rs                I/O operations
│       └── registration.rs      MDS registration
│
├── nfs_main.rs                  ← EXISTING (unchanged)
├── nfs_mds_main.rs              ← NEW: MDS binary
└── nfs_ds_main.rs               ← NEW: DS binary
```

## MDS Internal Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Metadata Server (MDS)                        │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              TCP Server (port 2049)                     │   │
│  │              (Tokio async I/O)                          │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         │                                       │
│  ┌──────────────────────▼──────────────────────────────────┐   │
│  │           RPC Message Handler                           │   │
│  │           (Sun RPC / XDR)                               │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         │                                       │
│  ┌──────────────────────▼──────────────────────────────────┐   │
│  │         COMPOUND Operation Dispatcher                   │   │
│  │         (Reuses existing NFSv4 dispatcher)              │   │
│  └─┬────────────────────┬────────────────────┬─────────────┘   │
│    │                    │                    │                 │
│    │ Standard Ops       │ pNFS Ops           │ State Ops       │
│    │                    │                    │                 │
│  ┌─▼──────────┐  ┌──────▼────────┐  ┌───────▼────────┐        │
│  │ File Ops   │  │ Layout Ops    │  │ Session Ops    │        │
│  │            │  │               │  │                │        │
│  │ • OPEN     │  │ • LAYOUTGET   │  │ • EXCHANGE_ID  │        │
│  │ • CLOSE    │  │ • LAYOUTRETURN│  │ • CREATE_SESSION│       │
│  │ • READ     │  │ • LAYOUTCOMMIT│  │ • SEQUENCE     │        │
│  │ • WRITE    │  │ • GETDEVICEINFO│ │                │        │
│  │ • GETATTR  │  │ • GETDEVICELIST│ │                │        │
│  │ • SETATTR  │  │               │  │                │        │
│  │ • CREATE   │  │               │  │                │        │
│  │ • REMOVE   │  │               │  │                │        │
│  └────────────┘  └───────┬───────┘  └────────────────┘        │
│                          │                                     │
│  ┌───────────────────────▼─────────────────────────────────┐   │
│  │              Layout Manager                             │   │
│  │              • Generate layouts                         │   │
│  │              • Track active layouts                     │   │
│  │              • Handle recalls                           │   │
│  │              • Policy: stripe, round-robin, locality    │   │
│  └───────────────────────┬─────────────────────────────────┘   │
│                          │                                     │
│  ┌───────────────────────▼─────────────────────────────────┐   │
│  │              Device Registry                            │   │
│  │              • DS endpoints                             │   │
│  │              • Capacity tracking                        │   │
│  │              • Health monitoring                        │   │
│  │              • Heartbeat processing                     │   │
│  └───────────────────────┬─────────────────────────────────┘   │
│                          │                                     │
│  ┌───────────────────────▼─────────────────────────────────┐   │
│  │              State Manager                              │   │
│  │              • Client sessions                          │   │
│  │              • Stateids (open/lock/layout)              │   │
│  │              • Leases                                   │   │
│  │              • Persistence (memory/ConfigMap/etcd)      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## DS Internal Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     Data Server (DS)                            │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              TCP Server (port 2049)                     │   │
│  │              (Tokio async I/O)                          │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         │                                       │
│  ┌──────────────────────▼──────────────────────────────────┐   │
│  │           RPC Message Handler                           │   │
│  │           (Sun RPC / XDR)                               │   │
│  └──────────────────────┬──────────────────────────────────┘   │
│                         │                                       │
│  ┌──────────────────────▼──────────────────────────────────┐   │
│  │         Minimal COMPOUND Dispatcher                     │   │
│  │         (Only READ, WRITE, COMMIT)                      │   │
│  └─┬────────────────────┬────────────────────┬─────────────┘   │
│    │                    │                    │                 │
│  ┌─▼──────────┐  ┌──────▼────────┐  ┌───────▼────────┐        │
│  │ READ       │  │ WRITE         │  │ COMMIT         │        │
│  │            │  │               │  │                │        │
│  │ • Validate │  │ • Validate    │  │ • fsync/flush  │        │
│  │   stateid  │  │   stateid     │  │                │        │
│  │ • Map FH   │  │ • Map FH      │  │                │        │
│  │   to bdev  │  │   to bdev     │  │                │        │
│  │ • Direct   │  │ • Direct      │  │                │        │
│  │   SPDK I/O │  │   SPDK I/O    │  │                │        │
│  └────┬───────┘  └───────┬───────┘  └────────────────┘        │
│       │                  │                                     │
│  ┌────▼──────────────────▼─────────────────────────────────┐   │
│  │              I/O Manager                                │   │
│  │              • Zero-copy I/O                            │   │
│  │              • Queue management                         │   │
│  │              • Buffer pooling                           │   │
│  └───────────────────────┬─────────────────────────────────┘   │
│                          │                                     │
│  ┌───────────────────────▼─────────────────────────────────┐   │
│  │              SPDK Interface                             │   │
│  │              • bdev_read()                              │   │
│  │              • bdev_write()                             │   │
│  │              • Direct NVMe access                       │   │
│  └───────────────────────┬─────────────────────────────────┘   │
│                          │                                     │
│  ┌───────────────────────▼─────────────────────────────────┐   │
│  │              MDS Registration                           │   │
│  │              • Register at startup                      │   │
│  │              • Periodic heartbeat                       │   │
│  │              • Capacity reporting                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Layout Generation Example

```
File: /data/bigfile.dat (16 MB)
Layout Policy: Stripe (4 MB chunks)
Data Servers: DS-1, DS-2, DS-3

┌────────────────────────────────────────────────────────────┐
│                    File Layout                             │
│                                                            │
│  Offset 0-4MB      ──────────────► DS-1 (nvme0n1)         │
│  Offset 4MB-8MB    ──────────────► DS-2 (nvme0n1)         │
│  Offset 8MB-12MB   ──────────────► DS-3 (nvme0n1)         │
│  Offset 12MB-16MB  ──────────────► DS-1 (nvme0n1)         │
│                                                            │
└────────────────────────────────────────────────────────────┘

Client reads 16 MB:
  1. Contacts MDS, gets layout
  2. Reads 0-4MB from DS-1    ┐
     Reads 4-8MB from DS-2    ├─ Parallel I/O
     Reads 8-12MB from DS-3   ┘
  3. Reads 12-16MB from DS-1

Result: 3x throughput (first 12 MB in parallel)
```

## Kubernetes Deployment

```
┌─────────────────────────────────────────────────────────────────┐
│                    Kubernetes Cluster                           │
│                                                                 │
│  Namespace: flint-system                                        │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  MDS Deployment (3 replicas for HA)                       │ │
│  │                                                            │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │ │
│  │  │ MDS Pod-1   │  │ MDS Pod-2   │  │ MDS Pod-3   │       │ │
│  │  │ (Leader)    │  │ (Follower)  │  │ (Follower)  │       │ │
│  │  └─────────────┘  └─────────────┘  └─────────────┘       │ │
│  │         │                │                │               │ │
│  │         └────────────────┴────────────────┘               │ │
│  │                          │                                │ │
│  │                  ┌───────▼────────┐                       │ │
│  │                  │  etcd Cluster  │                       │ │
│  │                  │  (State Store) │                       │ │
│  │                  └────────────────┘                       │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Service: flint-pnfs-mds                                  │ │
│  │  ClusterIP: 10.96.100.50:2049                             │ │
│  │  DNS: flint-pnfs-mds.flint-system.svc.cluster.local      │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  DS DaemonSet (one pod per node with storage)            │ │
│  │                                                            │ │
│  │  Node-1:                Node-2:                Node-3:    │ │
│  │  ┌─────────────┐        ┌─────────────┐      ┌─────────┐ │ │
│  │  │ DS Pod      │        │ DS Pod      │      │ DS Pod  │ │ │
│  │  │ device:     │        │ device:     │      │ device: │ │ │
│  │  │ ds-node1    │        │ ds-node2    │      │ ds-node3│ │ │
│  │  └──────┬──────┘        └──────┬──────┘      └────┬────┘ │ │
│  │         │                      │                  │      │ │
│  │         │                      │                  │      │ │
│  │  ┌──────▼──────┐        ┌──────▼──────┐    ┌─────▼────┐ │ │
│  │  │ nvme0n1     │        │ nvme0n1     │    │ nvme0n1  │ │ │
│  │  │ (SPDK bdev) │        │ (SPDK bdev) │    │ (SPDK)   │ │ │
│  │  └─────────────┘        └─────────────┘    └──────────┘ │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Namespace: default (user pods)                                │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Application Pods (NFS clients)                           │ │
│  │                                                            │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │ │
│  │  │ App Pod-1   │  │ App Pod-2   │  │ App Pod-3   │       │ │
│  │  │             │  │             │  │             │       │ │
│  │  │ mount:      │  │ mount:      │  │ mount:      │       │ │
│  │  │ /mnt/pnfs   │  │ /mnt/pnfs   │  │ /mnt/pnfs   │       │ │
│  │  └─────────────┘  └─────────────┘  └─────────────┘       │ │
│  │         │                │                │               │ │
│  │         └────────────────┴────────────────┘               │ │
│  │                          │                                │ │
│  │                Metadata: MDS Service                      │ │
│  │                Data I/O: DS Pods (direct)                 │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Configuration Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    Configuration Loading                        │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  1. Load Configuration                                    │ │
│  │                                                            │ │
│  │  Source Priority:                                         │ │
│  │    1. --config /path/to/pnfs.yaml  (highest)             │ │
│  │    2. PNFS_MODE=mds env vars                             │ │
│  │    3. Kubernetes ConfigMap (future)                       │ │
│  └───────────────────────┬───────────────────────────────────┘ │
│                          │                                     │
│  ┌───────────────────────▼───────────────────────────────────┐ │
│  │  2. Parse & Validate                                      │ │
│  │                                                            │ │
│  │  • Check mode: standalone / mds / ds                      │ │
│  │  • Validate required fields                               │ │
│  │  • Apply defaults                                         │ │
│  │  • Check consistency                                      │ │
│  └───────────────────────┬───────────────────────────────────┘ │
│                          │                                     │
│            ┌─────────────┴─────────────┐                       │
│            │                           │                       │
│  ┌─────────▼─────────┐       ┌─────────▼─────────┐            │
│  │  3a. MDS Mode     │       │  3b. DS Mode      │            │
│  │                   │       │                   │            │
│  │  • Load MDS config│       │  • Load DS config │            │
│  │  • Init device    │       │  • Init bdevs     │            │
│  │    registry       │       │  • Connect to MDS │            │
│  │  • Init layout    │       │  • Register       │            │
│  │    manager        │       │  • Start heartbeat│            │
│  │  • Load state     │       │  • Start I/O      │            │
│  │  • Start server   │       │    server         │            │
│  └───────────────────┘       └───────────────────┘            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## State Persistence Options

```
┌─────────────────────────────────────────────────────────────────┐
│                    State Persistence Backends                   │
│                                                                 │
│  Option 1: In-Memory (Dev/Testing)                             │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  MDS Process                                              │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │  DashMap<StateId, State>                            │  │ │
│  │  │  • Fast                                             │  │ │
│  │  │  • No persistence                                   │  │ │
│  │  │  • Lost on restart                                  │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Option 2: Kubernetes ConfigMap (Simple Production)            │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  MDS Process                                              │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │  In-Memory Cache                                    │  │ │
│  │  │  • Fast reads                                       │  │ │
│  │  │  • Periodic snapshots (every 30s)                   │  │ │
│  │  └──────────────────────┬──────────────────────────────┘  │ │
│  │                         │                                 │ │
│  │                         ▼                                 │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │  Kubernetes API                                     │  │ │
│  │  │  ConfigMap: flint-pnfs-state                        │  │ │
│  │  │  • JSON serialized state                            │  │ │
│  │  │  • Survives restarts                                │  │ │
│  │  │  • Simple to implement                              │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Option 3: etcd (HA Production)                                │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  MDS Pod-1        MDS Pod-2        MDS Pod-3              │ │
│  │  (Leader)         (Follower)       (Follower)             │ │
│  │     │                 │                 │                 │ │
│  │     └─────────────────┴─────────────────┘                 │ │
│  │                       │                                   │ │
│  │                       ▼                                   │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │  etcd Cluster (3 nodes)                             │  │ │
│  │  │  /flint/pnfs/devices/...                            │  │ │
│  │  │  /flint/pnfs/layouts/...                            │  │ │
│  │  │  /flint/pnfs/sessions/...                           │  │ │
│  │  │  • Distributed consensus                            │  │ │
│  │  │  • Strong consistency                               │  │ │
│  │  │  • Leader election                                  │  │ │
│  │  │  • Watch API for changes                            │  │ │
│  │  └─────────────────────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Summary

This architecture provides:

✅ **Separation of Concerns**: MDS handles metadata, DS handles data  
✅ **Scalability**: Add DSs independently for more throughput  
✅ **High Availability**: MDS replication with leader election  
✅ **Flexibility**: Multiple layout policies and state backends  
✅ **Performance**: Parallel I/O across multiple data servers  
✅ **Zero Impact**: Existing NFS server completely unchanged  

Ready for implementation! 🚀



