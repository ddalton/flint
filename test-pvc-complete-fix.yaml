apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc-complete-fix
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: flint-single-replica
