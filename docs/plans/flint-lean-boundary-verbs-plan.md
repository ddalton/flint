# flint-lean boundary verbs — sentinel protocol and gated manifest advance

**Status:** plan of record for the boundary-verbs tranche — **v2**, adversarial review folded (5 lenses: crash-takeover, gc-durability, security-dos, ops-fleet, model-drill; 43 findings, ledger in §9). No code.
**Peers:** `docs/plans/flint-lean-plan.md` (v2, the design of record for the sidecar), `docs/plans/flint-lean-chaos-drill.md` (12-leg drill + findings ledger), `lean/formal/README.md` (24-run TLC gate).
**Shipped substrate this plans against:** crate `flint-lean` at `lean/sidecar/` (bins `flint-sync`, `flint-lean-gateway`), operator at `spdk-csi-driver/src/lean_operator/`, chart `flint-lean-chart/`, model `lean/formal/LeanSubtree.tla`. (The sidecar moved out of `spdk-csi-driver/src/lean/`; any memory of that path is stale.)
**Hard constraint (user):** no regression on any radar axis under defaults. §7b is the axis-by-axis argument; every mechanism that could trade an axis is opt-in with the trade stated in the CRD. Review corrections to the constraint's own accounting: the draft's `perFilePublishCooldownSecs=300` default was itself a regression (now 0, §2.6) and the draft's idle-tick economics were wrong (§7 rebuilt in request units against the real 4-request tick).

---

## 1. Motivation and non-goals

### 1.1 The two problems

1. **Torn published views.** The barrier publishes on a clock (`floor_secs`, default 60 — `lean/sidecar/src/lib.rs:113-125`, run loop `bin/flint_sync.rs:143-174`). A multi-file logical change (write `model.json`, then `model.json.index`) can straddle a tick: the bucket manifest cites the first file and not the second. Every downstream reader — checkout of a new pod (`checkout.rs:38-161`), HITL GET through the gateway, a sibling cluster's sync — can observe a mid-logical-change state. Nothing in the shipped protocol lets the application say "here is a coherent point."
2. **Unbounded cross-pod freshness, agent-side.** A pod learns the bucket advanced only when its own barrier's HEAD-tick notices the manifest ETag moved (`barrier.rs:203-237`), and the *live tree* converges only via inbox consume at the next barrier or via the `sync` verb — which today is CLI-exec only (`bin/flint_sync.rs:71-141`) and **cannot even be exec'd beside a live `run` loop**, because the state-dir flock refuses a second process (`state.rs:87-137`, test `second_sidecar_on_one_tree_refuses`, `tests.rs:556`). An agent inside the app container has no way to ask for a sync and no way to learn there is news.

### 1.2 Why the filesystem is the interface

The app container ships **unchanged** — that is load-bearing in the lean plan. No HTTP client, no CLI binary, no library can be assumed in the app image. The only interface every agent is guaranteed to have is the shared workspace mount itself. So the boundary verbs are *files*: the agent touches `.flint/publish` or `.flint/sync`, and reads `.flint/publish.ack`, `.flint/sync.ack`, `.flint/remote.seq`. Richer doors (localhost/UDS HTTP, a gateway verb) are layered *over* this file protocol (§2.5), never instead of it.

**Trust statement (review: security-dos).** Every file in `.flint/` is writable by every process sharing the mount — the emptyDir is mounted into the sidecar *and* every app container (`inject.rs:84-98`), and there is no path the sidecar can write that the app cannot. Therefore **acks are advisory coordination signals, not attestations**: an in-pod process can forge `.flint/publish.ack`. The authoritative durability signal is a remote read the pod cannot forge — gateway `GET /status` (`last_cited_seq`) or the manifest itself. The agent contract must say this verbatim; agent libraries MUST NOT treat an ack as proof of remote durability for decisions that destroy local state. (A per-container MAC secret was considered and rejected: the pod has no intra-container trust boundary to anchor it — a process that can forge the ack can already tamper with the workspace bytes the ack describes. §9 SD-3.)

### 1.3 Non-goals (v1, DECIDED)

- **No interception.** No FUSE, no LD_PRELOAD, no fsync tracing. fsync-derived boundaries are named as a *future* coherent-point source for strict-journal synergy (§2.4.3) and are out of scope here.
- **No privilege.** Sentinels are ordinary files in the pod's own mount, writable by the workspace's own containers only.
- **No new remote surface for the app.** The app container gains zero network reachability. The optional UDS/localhost door (§2.5) is pod-internal and opt-in; the optional gateway verb is for HITL, not the app.
- **No change to one-writer-per-subtree.** The lease/claim/rotation machinery (`lease.rs`) is untouched — except that this tranche must *renew* the lease from its new arms (D12, §2.1a); the claim/rotation protocol itself is unchanged.
- **No history/branching.** A boundary is a coherent point in a single linear manifest sequence, not a snapshot, tag, or branch.
- **No scoped publish.** `.flint/publish` covers the whole workspace. (Scope exists only on `sync`, §2.2.)

---

## 2. Design

### 2.0 The `.flint/` control namespace — the precondition for everything else

**Shipped hazard (verified):** the scanner skips *only* the root-level `.flint-sync` state dir and symlinks (`scan.rs:32-50`). A workspace-local `.flint/` directory **is scanned today** and would be published to `<prefix>/files/.flint/...`. The gateway already reserves `.flint/` on the HTTP side (`path_ok`, `gateway.rs:110-118`), and the bucket-side control namespace is `LEAN_DIR = ".flint/lean"` (`lib.rs:59-65`) — but the scan does not.

**DECIDED — D0:** reserve `.flint/` at the workspace root as the local control namespace, symmetrical with the gateway's reservation:

1. `scan::walk` skips a root-level `.flint` directory exactly as it skips `.flint-sync`.
2. `scan::classify` **exempts `.flint/`-prefixed baseline paths from delete eligibility.** Without this, upgrading a workspace that legally published `files/.flint/...` under the old sidecar would see those paths absent from two consecutive scans (`scan.rs:83-108` two-scan rule) and publish their *deletion* — an upgrade must never delete data. Legacy `.flint/` citations are carried forward frozen (never re-uploaded, never deleted by us) and a one-time warning conflict record (`conflicts.jsonl`) names them.
3. `checkout` never materializes `files/.flint/...` citations into the local `.flint/` dir (they'd collide with control files); they are left cited and a conflict record names them.
4. **Pre-existing-data pre-flight (review: ops-fleet).** Reserving `.flint/` is a **breaking change** for a workspace already using it as data: its writes silently stop publishing, and — worse — files literally named `.flint/publish` or `.flint/sync` would be *consumed* (renamed away) by the sentinel poll: a data grab from a non-participating workspace. Rule: at `run` startup, if the baseline or manifest cites any `files/.flint/**` path, **or** `.flint/publish`/`.flint/sync` exist before `capabilities.json` has ever been written for this tree, sentinel consumption is **disabled** for the workspace: the poll arm never arms, `capabilities.json` reports `"verbs": []` with `"reason": "preexisting-flint-paths"`, and the condition `SentinelVerbsActive=False` (reason `PreexistingFlintPaths`) surfaces it fleet-visibly — not just a jsonl line inside the pod. The CRD knob `sentinels: auto|off|force` (§2.6) overrides: `force` requires the operator of record to accept the consumption behavior explicitly. The upgrade notes name the `.flint/**` publish-stop as breaking. Drill leg B22.

This is the keystone: with D0 in place, sentinel files can exist without ever entering the upload set, and workspaces that never touch `.flint/` see the scan get marginally *cheaper*.

### 2.1 Publish sentinel — `.flint/publish` and `.flint/publish.ack`

**The verb.** The agent creates or overwrites `.flint/publish`. Two forms:

- **Bare touch** (empty file): boundary with anonymous identity.
- **JSON body:** `{"nonce": "<agent-chosen string ≤128B>", "note": "<free text, ≤4KiB>"}`.

**Agent-side write discipline (review: crash-takeover).** Agents writing a JSON body MUST write-temp-then-rename *within* `.flint/` — a plain `open+write` racing the consume rename continues into the moved inode and leaves a torn body in the pending file. The sidecar tolerates this (below), but the contract requires atomic writes and the agent-library docs say so.

**Semantics (DECIDED — D1):** a sentinel with mtime T means *"everything ordered-before T is a coherent point; publish it."* The guarantee is **at-least**: the barrier that acknowledges the sentinel begins its scan strictly after consuming it, so every write visible on disk at consume time is in the published set. Writes racing the upload read are covered by the shipped pre-read-stat re-queue valve (`barrier.rs:672-680`) — the published state may include *later* bytes for a racing file, never earlier ones. The mtime-granularity residual of the scan (`scan.rs:73-75`) applies to the boundary guarantee too (§3). **Corollary (review: security-dos):** a sentinel-honoring barrier may never skip a dirty file for cost reasons — a cooldown that excluded a file's latest bytes would falsify the ack. Cost is bounded by *deferring the barrier* (D3), never by thinning its contents.

**Consumption discipline (DECIDED — D2, revised) — the inbox pattern, mirrored.** `consume_inbox` is exactly-once via integrate → persist → drop-in-CAS, idempotent between (`barrier.rs:67-157`, `inbox.rs:165-186`). The sentinel copies it with rename in place of CAS:

1. **Consume** = `rename(".flint/publish", ".flint-sync/publish.pending.json")` (same filesystem — both under the workspace emptyDir — so rename is atomic), wrapping the body with `{consumed_mtime_unix_ns, consumed_at, nonces: […]}`. A bare touch gets an empty nonce list entry. **Type check first (review: security-dos):** the poll `lstat`s the sentinel and consumes only `S_ISREG` — a FIFO, directory, symlink, or socket at the sentinel path is skipped with a warning conflict record (a FIFO would block the body read forever; the read is also bounded at 64 KiB). **Settle-before-consume (review: crash-takeover):** the poll arm never consumes while a pending file exists — a surviving pending must be honored, acked, and retired first, otherwise the rename would clobber the old pending record and orphan its nonces forever. Coalescing happens *inside* the pending record: while a pending sentinel waits for the min-interval, subsequent touches are consumed by appending their nonces to the standing pending record (rename onto a temp, merge, rewrite pending), never by overwriting it.
2. **Honor** = renew the lease (D12), then run one full fused barrier (`run_barrier`, all 7 steps, `barrier.rs:180-469`). In gated mode this barrier is a *citation* pass (§2.4). **Torn-body rule (review: crash-takeover):** an unparsable or oversize pending body is honored as a bare-touch boundary (nonces empty, mtime from the file) with a warning conflict record — never a wedge, never a silent drop. A pending file, parsable or not, is "work": it defeats the skip-on-no-diff fast path exactly as `repairs_pending` does (`barrier.rs:203-206`).
3. **Ack** = after the barrier's manifest CAS installs and the baseline rewrite completes (step 7), write `.flint/publish.ack` via atomic write-then-rename:

```json
{
  "status": "ok",                      // "ok" | "refused-fenced"  (review: crash-takeover)
  "nonces": ["…", "…"],                // EVERY coalesced nonce covered (bounded 32, oldest dropped)
  "sentinel_mtime_unix_ns": 1756…,     // latest covered touch — bare-touch agents match on this
  "seq": 4132,                         // manifest seq installed by the honoring barrier
  "manifest_etag": "…",
  "boundary": "sentinel",              // "sentinel" | "sentinel-deferred" (budget, D3)
  "completed_unix": 1756…,
  "observed_epoch": null,              // set on refused-fenced: the epoch that fenced us
  "report": { "uploaded": 12, "deleted": 1, "parked": 0, "consumed": 2, "no_change": false }
}
```

   The ack carries the **full covered-nonce set** (review: security-dos): under coalescing, an agent whose nonce rode behind a later touch would otherwise never see its nonce and re-touch in a loop, feeding the storm the rate limit exists to prevent.
4. **Retire** = delete `.flint-sync/publish.pending.json` *after* the ack rename.

**Refused acks (review: crash-takeover — this was the largest protocol hole).** The draft had no negative ack anywhere; deposal stranded sentinels forever. Shipped behavior: `run_barrier`'s `verify_not_deposed` returns `Fenced` (`barrier.rs:190`), `run_loop` exits on it (`bin/flint_sync.rs:161`), `main` exits 0 (`bin/flint_sync.rs:84`), and the restarted claimant spins in `Waiting` against the live successor — while the agent container keeps running, its pending sentinel never answered, `capabilities.json` still advertising live verbs. Rules:

- On `Fenced` during honor (or during the drain, D10), the sidecar writes the ack with `status: "refused-fenced"` — echoing nonces/mtime, naming the observed epoch — **before** the fenced exit, then retires the pending file.
- The restarted process, upon `claim_step` observing a foreign live holder with a surviving pending file, writes/refreshes the refused ack (it can never honor).
- On fence, `capabilities.json` is atomically rewritten with `"state": "fenced"` and `"verbs": []`, and `gauges.json` says the same, so agents stop touching sentinels on a zombie.
- **In-loop sync honor is gated on `verify_not_deposed`** (review: crash-takeover): `Sidecar::sync` has no lease/epoch check at all (verified — `sync.rs` contains none; today it is only reachable via `claim_then`, which claims first). A straggler consuming a sync sentinel between deposal and its next cooperative fence would apply the successor's manifest onto its zombie tree and ack SUCCESS. The select-arm honor path calls `verify_not_deposed` first and refuses with `refused-fenced` on failure.
- Model product 1 gains the deposal arm; drill leg B18.

**Crash matrix (review: crash-takeover — the draft's per-crash-point prescriptions were wrong and are replaced).** The draft said a restart finding pending-and-no-ack could "write the ack from the pending record against the current baseline seq (the install already happened)". It cannot know that: pending-present-and-no-matching-ack is the *same observable state* for crash-mid-step-2 (before the manifest CAS — baseline is rewritten only at step 7, `barrier.rs:443-456`) and crash-after-step-7; acking from persisted state in the first case asserts publication of writes that never uploaded, violating `Inv_AckImpliesCited`. **One uniform rule replaces the matrix:** on restart, pending + no ack whose nonces/mtime match ⇒ **always run a full barrier** (idempotent; AdoptOwn/recent-uuids covers a half-published set, `barrier.rs:564-638`), then ack with *that* barrier's installed seq — never ack from persisted state alone. Crash after ack before retire → retire on restart (nonce match, no second barrier — the ack already names a real install). Settle-before-consume (rule 1) guarantees a fresh sentinel can never clobber the surviving pending. Pod *replacement* loses the pending file with the emptyDir — and also the workspace; the successor's checkout starts from the last coherent manifest, and the agent (also restarted) re-declares. Both hazards are TLC mutations in product 1: ack-from-stale-baseline must violate `Inv_AckImpliesCited`; consume-over-pending must violate `Inv_NoNonceOrphan`.

**Coalescing + rate limit (DECIDED — D3, revised).** Sentinel consumption is a poll tick (default 1 s, env `FLINT_SYNC_SENTINEL_POLL_SECS`) that `lstat`s exactly two fixed paths — no inotify dependency in v1, no directory scan, ~2 lstats/s when idle. **Loop structure (review: security-dos — the draft's structure was self-starving):** the shipped run loop recreates `tokio::time::sleep(floor)` inside `select!` on every iteration (`bin/flint_sync.rs:151-155`), and `lease::renew`'s only call site is that floor arm — a third arm completing every 1 s would win every iteration, perpetually reset the floor sleep, and the lease would **never renew**: the sidecar deposes itself into the 7,591-PUT straggler class by construction. The loop is rewritten on **independent non-resettable `tokio::time::interval` timers** — renewal interval, floor interval, poll interval — so no arm's readiness can starve another. Drill leg B17 proves renewal under sentinel storm.

Rate limiting is two-level, enforced sidecar-side (never on agent honor):
- `sentinelMinIntervalSecs` (default 5): a consumed sentinel arriving sooner than this after the previous sentinel-honoring barrier waits; touches during the wait coalesce into the pending record (D2.1), and the ack is cumulative — any ack whose `sentinel_mtime_unix_ns ≥` your touch covers your boundary, because a later boundary strictly contains an earlier one.
- `sentinelHourlyBudget` (default 60) (review: security-dos): min-interval alone still permits 17,280 forced barriers/day — a 12× request amplification per hot small file, agent-controlled (§7). When the hourly budget is exhausted, sentinel honors **defer to the next floor tick**: the boundary is still honored by a real barrier (D1's corollary — contents are never thinned), the ack is stamped `boundary: "sentinel-deferred"`, and `gauges.json` exposes the exhaustion. Sustained forced-barrier rate is thereby capped near cadence-equivalent; bursts up to the budget are prompt.

**Mixed-fleet hazard (must be loud in agent docs):** on an **old** sidecar, `.flint/publish` is a regular file — it gets scanned and *published to the bucket* (the D0 hazard). Agents MUST check the capability marker (§2.5) before touching any sentinel.

#### 2.1a Lease-renewal discipline (DECIDED — D12, new) (review: crash-takeover, security-dos)

The lease token advances only via `lease::renew`, and a standby judges the holder dead after `QUIET_POLLS=6` observations 10 s apart (`lease.rs:24,66`) — a ~60 s quiet threshold. The draft's sentinel and citation barriers never renewed; on the plan's own 1-hour-floor drill fixture, a workspace publishing continuously via sentinels would present a token quiet for up to an hour and any replacement claimant would depose the *live* writer within ~60 s — mid-honor, feeding the refused-ack path unnecessarily. Rules:

1. Every barrier-triggering arm — floor tick, sentinel honor, sync honor, citation pass (both sources), drain — calls `lease::renew` first, exactly as the floor arm does today.
2. Independently, a **heartbeat renewal interval** fires at `min(floor_secs, (QUIET_POLLS×10)/2) = min(floor, 30) s`, decoupling liveness signaling from publish cadence entirely. (At the default floor=60 the shipped renew cadence *equals* the takeover threshold — already racy; this closes that too.)
3. Renewal runs on its own non-resettable interval (D3) — structurally immune to arm starvation.

Cost: at floor=60 this changes the renewal PUT cadence from 60 s to 30 s — priced in §7, and the one deliberate default-posture request increase in this plan (+1 PUT/min/workspace), bought for takeover-safety of the shipped protocol as much as the new arms. Drill leg B17.

### 2.2 Sync sentinel — `.flint/sync` and `.flint/sync.ack`

**The verb.** `.flint/sync`, empty or `{"nonce": "…", "scope": ["inputs/", "shared/config.json"]}`. Same consume/honor/ack/retire discipline as §2.1 (own pending file `.flint-sync/sync.pending.json`, own min-interval sharing `sentinelMinIntervalSecs`, same type-check/torn-body/settle-before-consume/refused-fenced rules, same budget pool).

**Why in-loop:** the flock forbids a second `flint-sync` process beside a live `run` (`state.rs:116-136`), so the sentinel is consumed inside the run loop, invoking the existing `Sidecar::sync` (`sync.rs:22-143`) at the sidecar's own safe point — never concurrent with a barrier, never in background, and **only after `verify_not_deposed` passes** (D2). This *preserves* the design invariant that sync mutates the live tree only when invoked: the agent's touch is the invocation, placed at an agent-chosen safe point.

**Scope semantics (DECIDED — D4, with a correctness rule that is not optional):** scope is a list of path prefixes (and exact paths); remote adds/changes/deletes apply only to in-scope, locally-clean paths, exactly as today's whole-tree rules (`sync.rs:31-143`: scan-first, locally-dirty wins, deletions only on clean paths, every skip a conflict record).
**The rule:** a scoped sync advances `inst_base` **only for the paths it actually applied or verified in scope**, and leaves `baseline.seq` / `baseline.manifest_etag` **untouched**. Rationale: `inst_base` is the three-way merge base (`state.rs:28-48`; baseline≠instBase is a load-bearing model finding, `lean/formal/README.md:81-85`). If a scoped sync advanced the whole `inst_base` to bucket-current, every out-of-scope foreign change would look already-integrated to the next merge (`manifest.rs:118-124`) and would be **silently lost from the inbox flow forever**. With D4, out-of-scope changes remain "foreign" and flow through the normal merge → inbox → consume path at the next barrier (`barrier.rs:368-467`), untouched from today. Whole-tree sync (no scope) behaves exactly as shipped, including advancing seq/etag. Because D4 rewrites the per-path semantics of the exact object the model has refuted naive designs on twice, it is **modeled before code** — product 4, §4 (review: model-drill).

**Scope validation + write containment (review: security-dos).** Scope is agent-controlled JSON driving writes by the credential-holding sidecar, and the write path it invokes — `write_file_atomic` (`barrier.rs:702-724`, also used by consume and checkout) — does `create_dir_all(parent)` + `fs::write` with no `O_NOFOLLOW` and no root-containment check, while the scanner skips symlinks (`scan.rs:40-43`) so a planted symlink is invisible to the sidecar. An unprivileged app that plants `inputs -> /root/.aws`, lands a HITL object at `inputs/<path>`, and drops a scoped sync turns the sidecar into an on-demand arbitrary-file-write primitive outside the workspace. Rules (Phase 2 gate; the hardening also covers the pre-existing checkout/consume exposure):
- `write_file_atomic` becomes containment-safe: openat-style parent walk (or canonicalize-and-verify-prefix) refusing any target that escapes the workspace root or traverses a symlink component.
- Scope entries are normalized and matched on **path-component boundaries** (`"in"` never matches `internal/`); entries whose parents are symlinks are refused with a conflict record; scope list bounded (≤64 entries, each ≤1 KiB).

**Ack payload:** the existing `SyncReport {applied, deleted, conflicts, seq}` (`sync.rs:22-28`, already `Serialize`) extended with `status`, `nonces`, `sentinel_mtime_unix_ns`, `scope`, and `out_of_scope_foreign: <count>` (changes seen but deferred to the inbox flow). **The conflict report rides the ack in full** — "never a silent winner" must survive the file transport: every dirty-vs-remote skip appears as a `ConflictRecord` in `sync.ack`'s `conflicts[]`, in addition to `conflicts.jsonl` as today.

**Phantom-conflict rule on re-honor (review: crash-takeover):** `sync()` applies foreign files one by one and saves the baseline only at the end (`sync.rs:87-101,140`). A crash mid-apply followed by re-honor makes already-applied paths scan dirty against the stale baseline and refuse with `sync-dirty` conflicts — the ack would report conflicts for paths whose local bytes *are* the remote bytes, and those paths would then re-publish as spurious generation bumps. Rule: before declaring a `sync-dirty` conflict, compare content identity (crc64 of the local file vs the remote entry's `crc64_b64`, already in `LeanEntry`); byte-identical paths are treated as applied — the baseline entry advances instead of a conflict recording. Unit test injects the mid-sync crash.

### 2.3 `.flint/remote.seq` — the news ticker

**DECIDED — D5, revised.** The sidecar maintains `.flint/remote.seq` (write-then-rename), updated from information it already has — **zero added bucket requests**:

- At the no-change fast path, the barrier already HEADs the manifest (`barrier.rs:214-236`; HEAD-not-GET is the measured 27 s/1.3 GiB lesson). The HEAD's `ObjectMeta.meta` carries the `flint-gen` stamp, and `cas_write` stamps `generation = m.seq` (`manifest.rs:92-99`) — so the observed remote seq comes off the existing HEAD for free.
- At every full barrier completion, from the installed manifest itself.

```json
{ "observed_seq": 4140, "observed_etag": "…", "integrated_seq": 4132, "updated_unix": 1756… }
```

`observed_seq > integrated_seq` means "there is news you have not integrated — consider dropping a `.flint/sync` sentinel at your next safe point." The agent polls one **local** file: no network, no client, no credentials. **Heartbeat rule (review: ops-fleet — the draft's two claims contradicted):** the draft said the file is written only on content change *and* that `updated_unix` is the staleness tell — on an idle-but-healthy workspace those can't both hold (an agent couldn't distinguish "no news" from "sidecar dead"). Rule: `updated_unix` is refreshed on **every successful HEAD tick** (a local rename — zero bucket cost); `observed_seq`/`observed_etag` change only when they change. Agent-side staleness rule, in the contract: `updated_unix` older than 3×floor ⇒ sidecar or proxy problem — check `gauges.json`. Freshness of *news* is bounded by the floor tick; if the proxy is unreachable the ticker goes *stale, never wrong* (the gateway-vs-proxy failure split, chaos C8/C12, is unchanged: remote.seq survives a gateway outage, dies-stale with the proxy).

### 2.4 Gated manifest advance — durability split from visibility

#### 2.4.1 The three modes (DECIDED — D6)

`boundaryMode: cadence | hybrid | gated`, **default `hybrid`**.

- **cadence** — exactly today: every floor tick runs the fused barrier (upload + cite in one pass). Sentinels still work (a sentinel triggers a fused barrier); this mode exists as the explicit "old behavior" escape hatch. (Propagation caveat: switching a live workspace's mode requires pod recreation — §2.6.)
- **hybrid (DEFAULT)** — the fused barrier runs at every floor tick *and* at every consumed publish sentinel, **whichever comes first**. Citation never waits: published-view freshness is never later than today's, and boundaries only add coherent points. There is **no separate eager lane in hybrid** — uploads and citations coincide, so the uncited-object set is exactly today's crash-window set (the intent journal's keys, `state.rs:61-71`), bounded by one cadence and reconciled by the shipped AdoptOwn/recent-uuids machinery (`barrier.rs:564-638`). This is what makes constraint (b)'s byte clause true by construction under defaults; the request clause is D3's budget (review: security-dos). Drill leg B16 is the regression proof that the rewritten loop still fires the floor barrier with no sentinel present (review: model-drill).
- **gated (opt-in per workspace)** — the two lanes split:
  - **Upload lane (durability), every floor tick:** renew lease (D12), verify epoch, consume inbox (unchanged — inbound HITL adoption stays exactly today's behavior), scan, stage-diff, guarded uploads — but PUTs land in the **staging namespace**, and no manifest CAS runs. **The lane opens no HITL window** (review: crash-takeover, security-dos): the window exists to fence HITL out of the upload→CAS span (`inbox.rs:89-94` refuses gateway writes while a live window stands, cleared only by step 7's CAS, `barrier.rs:467`); a lane with no CAS that opened a 180 s-deadline window every 60 s would refuse HITL admission essentially forever between citations — and it protects nothing, since staged PUTs land in a namespace HITL never contends on. Window open/clear belong exclusively to the citation lane. RPO for bytes stays at the floor. **Stage-diff base (review: ops-fleet):** the scan diffs against **baseline ∪ pending** — a path whose scan (size, mtime) matches its pending entry is *not* re-staged. Diffing against the baseline alone (which gated mode leaves un-advanced until citation) would re-PUT every staged-but-quiet file every tick: 50 quiet files × 60 ticks = 3,000 PUTs/hour vs today's 50, falsifying §7. Unit test: no re-stage of an unchanged staged file across N ticks. When a path *is* re-staged, the lane **deletes the superseded stage key** (it is in the pending record; DELETE is free) — staging holds at most one generation per path plus crash remnants (review: gc-durability).
  - **Citation lane (visibility), at coherent points only:** a full fused barrier that copies staged objects into place, installs the manifest with the *entire* pending set in one CAS, runs GC, rewrites the baseline, then deletes the cited stage keys. Coherent points: **(i)** a publish sentinel; **(ii)** bounded quiescence — defined as **scan-to-scan stability**: no path's (size, mtime) changed between consecutive upload-lane ticks and nothing new appeared, for `quiesceBoundSecs` (review: gc-durability — the draft's "no scan diff vs baseline" definition could never fire once anything was pending, since pending paths stay classified-changed until citation: quiescence would have been dead code and every citation would ride the lag cap); **(iii)** the lag cap — `visibilityLagBoundSecs` elapsed since the last citation forces one even mid-change, stamped `boundary: "forced-lag-cap"`; **(iv)** the backlog cap — staged-uncited work exceeding `stagedBacklogCapObjects`/`stagedBacklogCapBytes` forces a citation, bounding the preStop drain to a sized backlog (review: ops-fleet, security-dos). (v — future, out of scope: fsync-derived boundaries; the enum reserves `"fsync"` as a source name only.) The report always says which source fired, and **the source is stamped bucket-visibly** — `cas_write` meta gains `flint-boundary-source` alongside `flint-gen` (review: ops-fleet): downstream consumers (checkout, sibling sync, HITL) can distinguish a declared-coherent citation from a forced possibly-torn one *from the bucket alone*, and the fleet can count chronic forced citations. Drill B10 asserts the stamp.
  - CRD validation: `gated` **requires** `visibilityLagBoundSecs` set — unbounded staleness is impossible by construction — plus the ageout cross-validation in D8.

#### 2.4.2 The staging namespace — why gated uploads cannot go in place (DECIDED — D7, revised)

`upload_one` PUTs to `files/<path>` guarded on the recognized baseline etag (`barrier.rs:564-638`). If a gated eager upload overwrote `files/<path>` in place while the manifest still cited the old etag, the *cited* object would be destroyed: checkout's If-Match fetch would 412 and its S3-wins arm would adopt the **current mid-logical-change bytes** (`checkout.rs:102-121`) — exactly the torn view gating exists to prevent. So in gated mode:

- Eager uploads go to `.flint/lean/eager/<flush_uuid>/<path>` — inside the reserved bucket namespace (`LEAN_DIR`, `lib.rs:59-61`), which is never scanned, never checked out, never GC'd (`barrier.rs:412-441` GC touches only delete-classified `files/` paths; drill C10 proves prefix isolation).
- The citation pass `CopyObject`s the latest staged generation to `files/<path>` (guard chain identical to `upload_one`: If-Match on recognized etag, AdoptOwn, park on foreign — the 412 policy inherited), then installs the manifest citing the copied objects' etags, in one CAS. **AdoptOwn recognizer pinned (review: gc-durability):** gated mode mints one lane uuid per tick — easily more than the 8-entry `recent_uuids` cap (`state.rs:198-212`) between citations, so a crash-and-retry of the citation pass relying on `recent_uuids` alone would park its *own* copy as foreign. The citation pass (and the preStop fused barrier) adopt-own via `pending[path].flush_uuid` + crc64 confirmation; `recent_uuids` is fallback only. TLC mutation in product 2.
- **The pending-citation record** lives in its **own file** `.flint-sync/pending.json` (temp+rename discipline, `state.rs:180-212`): `{path → {stage_key, etag, crc64, size, mode, mtime, flush_uuid, base_etag, staged_unix}}`. **Not** grafted into `intent.json` (review: crash-takeover): `clear_intent_keys` load-modify-saves that file at every barrier's step 7 and an implementer reusing the shipped helper would wipe the pending map at the first citation, orphaning every staged object. A test pins that `clear_intent_keys` leaves `pending.json` intact. Pending entries defeat the skip-on-no-diff fast path the same way `repairs_pending` does.
- **Write ordering (review: crash-takeover):** stage-PUT first, pending-record second. Crash between them leaves an orphan staged object (ageout's side); the reverse order would leave a pending entry citing a stage key that does not exist.
- **Citation-time re-validation (review: model-drill — the gated×sync amputation):** a pending entry records `base_etag`, the recognized baseline etag at staging time. If, by citation time, `baseline[path].etag ≠ pending[path].base_etag` — a HITL consume or sync applied remote bytes and advanced the baseline after staging (e.g. the agent reverted the file and a foreign write landed) — the staged generation is **stale**: copying it would overwrite the foreign bytes with an If-Match that *passes* (the guard checks the current recognized etag), a HITL amputation with no conflict record. Rule: re-scan the path; if the live file still differs from baseline, re-stage from the live tree and cite that; else drop the pending entry with a conflict record naming the superseded stage. TLC mutation: citation copies a pending entry whose base etag advanced — must violate `Inv_HITLDurable`.
- **Stage-NotFound arm (review: gc-durability, ops-fleet):** `CopyObject` on a reaped stage key (lifecycle won a race — a pod fenced/CrashLooping past ageout, a >ageout proxy outage) must not wedge or silently drop the path: re-stage from the local file (still on disk) under the current lane uuid and cite that; if the local file is gone or no longer differs from baseline, drop the pending entry with a conflict record naming the loss. Proactively, the upload lane **refreshes** (re-PUTs) any stage object whose `staged_unix` age approaches `eagerOrphanAgeoutDays − 1 floor`, so a live pending record never cites a key lifecycle is about to reap. Model product 2 gains the ageout-races-pending action; gauge `staged_age_max_secs`.
- **Citation-intent document + roll-forward (review: gc-durability, model-drill — critical):** the CAS is atomic but the N copies before it are not. During the copy phase, `files/` holds boundary-new bytes under a manifest citing old etags — a reader in that window can 412 into S3-wins and materialize new-A + old-B; and a crash **with pod replacement** mid-copy kills the pending record with the emptyDir, after which the successor's checkout S3-wins-adopts the mixture and the citation-repair pass (`barrier.rs:317-364`) would re-cite the half-boundary into the manifest *permanently* — the mid-logical-change state wearing a boundary's name. Rule: before the first copy, the citation pass writes a small **citation-intent document** at `<prefix>/.flint/lean/citation-intent.json` (stage keys + target paths + expected etags + flush_uuid + epoch), deleted after the CAS. A successor's claim path (and checkout) that finds a standing citation-intent **rolls the citation forward** from the staged set — every source is durable in staging — instead of adopting a torn mixture. This closes the crash/permanent case; the *transient* live-reader window remains, stated as a residual (§3.11) with Q2 (versioned staging) as the structural closure. Model product 2 mutation: successor ignores the intent doc — must violate `Inv_BoundaryAtomic`. Drill leg B15.
- **Local deletes are visibility too:** in gated mode, delete classifications are *withheld* from the manifest until a citation pass (recorded in `pending` as tombstone intents); GC of the deleted `files/` objects runs, as always, only after the CAS that uncites them (`barrier.rs:412-441` — deletes LAST is model-pinned).
- **HITL exemption (DECIDED):** inbox consume and the citation-repair pass are *not* gated — a HITL write is already a coherent, whole-object act by a foreign author; delaying its re-citation would regress the shipped `hitl_upload_survives_two_barriers_without_sync` invariant. Repairs ride the next citation pass **or**, if none is due within one floor, trigger a repair-only citation (no local upserts). **Repair-only passes withhold pending tombstones** (review: model-drill): installing withheld deletes without their paired staged creates would make a rename r→s reader-visible as r-gone/s-absent at an undeclared point — a torn rename worse than today. The repair-only pass installs only the repair citations; the invariant is pinned in product 4.

**Two safety properties fall out of D7, with their honest scope (review: crash-takeover):**
1. **GC can never eat an uncited eager object** — barrier GC only ever addresses `files/` delete-classified paths; staged objects are in a namespace it never touches. The "GC grace class" is a *namespace*, not a flag on the GC path. **The dual invariant is load-bearing for D8 (review: gc-durability): no manifest entry's key may ever lie outside `files/`.** `LeanEntry.key` is an arbitrary string; nothing structural stops a future "cite the staged object in place, skip the copy" optimization or a recovery tool from citing `eager/` keys — at which point the lifecycle rule becomes a delayed destroyer of *cited* data, fleet-wide, three days after the change ships green. Enforced in `manifest::cas_write` (refuse an entry whose key is under `LEAN_DIR`), stated in D8, pinned in product 2 with the cite-in-place mutation required to fire. Recovery and preStop always install staged bytes by copy-then-cite, never cite-in-place.
2. **The deposed-straggler eager class shrinks — for `files/` data keys only (review: crash-takeover — the draft overclaimed).** Chaos C3 measured **7,591** post-deposal data PUTs to `files/` — uncited orphans that overwrite real keys (`docs/plans/flint-lean-chaos-drill.md:132`; the 7,624 figure sometimes quoted is not what the doc records). Under gated staging, a deposed straggler's eager PUTs land only in its own `eager/<flush_uuid>/` prefix — they cannot touch cited `files/` objects even before proxy-side P5 lands. But the lane still touches the **shared inbox cell**: `open_window` checks only the doc's embedded window epoch, not the epoch cell (`inbox.rs:136-149`); `drop_entries` can remove HITL entries the straggler consumed into its doomed tree; `preserve_conflict_copy` PUTs conflict keys. Containment claims are therefore scoped to `files/` data keys, and the lane additionally: re-reads the epoch cell immediately before `open_window`/`drop_entries` (cheap read, shrinks the TOCTOU — it cannot close it; P5-at-the-proxy remains the fence, chaos finding 2), and `open_window`/`clear_window` compare against the epoch **cell**, not just the embedded window. B12's oracle snapshots the inbox doc before freeze and after thaw and asserts no entry disappeared and no stale-epoch window appeared. Model product 3 gains the straggler-drops-successor-inbox interleaving. Hybrid/cadence keep today's direct-PUT exposure; P5 remains the plan of record for those.

#### 2.4.3 Orphan ageout (DECIDED — D8, revised)

Staged objects become orphans when the pod is replaced (pending record dies with the emptyDir) or a straggler is deposed — **and only then**: superseded-generation deletes and post-CAS stage deletes (§2.4.1/§2.4.2) make orphaning exceptional, not steady-state (review: gc-durability — without them, a hot 10 MiB file staged every floor would strand ~1,440 objects/day × ageout ≈ 42 GiB of staging for one file, and every staging alarm would drown in normal-operation noise). Lifecycle ageout is the **backstop for crash windows only**: a rule on `<prefix>/.flint/lean/eager/` with age `eagerOrphanAgeoutDays` (default 3). A lifecycle rule scoped to the staging prefix *cannot* touch cited data — under invariant D7.1 — which a naive "age floor on uncited objects" could never guarantee (age cannot distinguish cited from uncited in `files/`).

Corrections folded:
- **Versioned buckets (review: gc-durability):** flint-store's bootstrap treats versioning=Enabled as the *recommended* posture and warns loudly when off (`s3.rs:418-433`). On a versioned bucket a plain Expiration rule only inserts delete markers — every staged generation would persist as a noncurrent version indefinitely: reclamation would be a fiction exactly where the fleet is configured correctly. The rule is **Expiration + NoncurrentVersionExpiration + ExpiredObjectDeleteMarker** on the staging prefix; the operator sweep fallback issues **version-scoped deletes**; and the Phase 3 conformance probe checks `DeleteObjectVersion` is permitted (a project-scoped proxy may deny it — that blocks gated mode the same way a failed CopyObject probe does). Drill B21's versioned-MinIO variant asserts staged bytes actually disappear (version count, not just LIST).
- **Provisioning discipline (review: gc-durability):** `PutBucketLifecycleConfiguration` is full-replace; the eager rule follows `bootstrap_lifecycle`'s read-merge-append discipline (`s3.rs:763-840`) — never clobbering the MPU-abort rule or user rules.
- **The age floor is pinned to what it must exceed (review: gc-durability):** the maximum legitimate pending lifetime. CRD cross-validation: `eagerOrphanAgeoutDays×86400 > K×(visibilityLagBoundSecs + floor_secs)`, K=2, refused via `BoundaryModeAccepted=False`. Outages longer than the floor are covered by the re-stage refresh + NotFound arm (D7).
- **The sweep fallback uses `eagerOrphanAgeoutDays`, never the MPU sweep's 3600 s** (`reconcile.rs:151-155` — reusing that constant would eat live pending stages under any lag bound > 1 h). Where the deployment proxy cannot express lifecycle rules, the operator sweeps the staging prefix on its 1800 s pass (prefix-scoped list, small in steady state by construction).

**Takeover + DR posture (DECIDED — D9, revised) (review: gc-durability, ops-fleet).** A successor's claim path lists the staging prefix once and *surfaces* orphan candidates — it never auto-adopts (auto-adoption would install a possibly-mid-logical-change set; the sole exception is rolling forward a standing citation-intent, D7, which is a *declared* boundary). Revisions:
- Surfacing is made **durable and fleet-visible**, not emptyDir-local: at claim time (and each citation) the sidecar writes an orphan-candidate summary under `<prefix>/.flint/lean/orphans.json` (survives cluster loss), and the operator raises an event + condition when the staging prefix is non-empty with **no live lease** — the DR window is exactly when conflicts.jsonl and the CR no longer exist.
- **Recovery tooling is v1, not deferred** — `flint-sync recover-staged` (exec/operator-action) installs a *named* staged set as one flagged boundary (`boundary: "recovered"`, conflict records throughout, copy-then-cite per D7.1). Rationale: this fleet is pure-spot by standing directive — pod replacement is the *routine* path, not the rare crash, so "surface-only, tool later" made gated mode's RPO trade routinely unrecoverable, and the mode-change escape hatch itself manufactures orphans (§2.6). Phase 4 deliverable.
- **DR statement, verbatim (also in the CRD doc-comment): uncited = invisible to every import, DR checkout, GitOps re-apply, and cross-cluster move** — the operator explicitly designs for CR delete/recreate over the same data (`reconcile.rs:4-16`), and all of those see only the cited manifest. DR runbook guidance: suspend or raise the staging lifecycle rule during recovery (the 3-day clock runs through the disaster) — §8 Q1 asks how automatic that should be.

#### 2.4.4 preStop rule (DECIDED — D10, revised)

The SIGTERM arm's final drain barrier runs **fused and cite-everything in every mode**: all pending citations copied and installed (copy-then-cite — never cite-in-place, D7.1), all dirty files uploaded and cited, regardless of boundaries — then lease release. Revisions (review: crash-takeover, security-dos, gc-durability, ops-fleet — the draft's drain was unsized, un-retried, and settled nothing):

1. **The drain settles every owed ack before lease release:** a pending sentinel at SIGTERM is answered by the drain itself (`status: "ok"` after the fused install — the container-restart case where emptyDir and agent survive must not strand a waiting agent), or `status: "refused-fenced"` if the drain hits Fenced (the shipped path propagates Fenced with no ack and exits 0 — that branch now writes the refusal first).
2. **Bounded retry:** the shipped arm makes one attempt and releases the lease even on failure (`bin/flint_sync.rs:165-171` — `let _ = lease::release`). The drain retries the final barrier within the remaining grace budget before releasing; a transient store error no longer silently forfeits everything since the last boundary.
3. **Sized against grace:** gated's drain adds a per-*object* term (one CopyObject per pending path, latency-bound through the proxy) that the parent plan's bytes×rate drain arithmetic (`flint-lean-plan.md:194-204`) lacks — and the injected sidecar sets no `terminationGracePeriodSeconds` at all today, so it inherits the 30 s default; under a SIGTERM-ignoring agent, native-sidecar ordering burns the agent budget first. Rules: the webhook derives a grace bump from the configured caps × measured copy rate + slack (the startupProbe-derivation precedent, `inject.rs:40-54`); the CRD **refuses** gated mode when derived need exceeds configured grace (condition, same pattern as gated-requires-lag-bound); the **backlog-cap citation source (§2.4.1.iv) bounds the drain to a sized backlog by construction**; and the parent plan's early-drain hook runs a citation pass at deletionTimestamp observation so the final fused barrier handles only the last delta. On pure-spot fleets the ~2-minute reclaim cap is the routine ceiling — the arithmetic is run against it, and the gated CRD doc-comment states the spot-reclaim RPO consequence verbatim.
4. The cluster drill's A1 lossless-drain leg is the acceptance oracle; under gated mode the leg's precondition is *observed staged-uncited work*, and B11 gains a SIGKILL-mid-drain variant (recovery lands at the last boundary; orphans surfaced per D9) and a SIGTERM-ignoring-agent variant (the parent plan mandates that control for drain legs).

### 2.5 Layered doors and the capability marker

**Capability marker (DECIDED — D11, revised):** `.flint/capabilities.json`, written atomically **at every `run` startup — after claim succeeds, before the first poll** (review: crash-takeover — the draft pinned the write inside checkout completion, but the live-tree restart row returns at `marker_present()` (`checkout.rs:40-44`) without reaching it: an in-place sidecar image update would upgrade a fleet whose live workspaces never get the marker, leaving sentinels dead on exactly the pods the upgrade targeted). Also written during first checkout before the checkout-complete marker, so it exists the instant the agent may start.

```json
{ "protocol": 1, "verbs": ["publish", "sync", "remote-seq"], "boundary_mode": "hybrid",
  "state": "live",                       // "live" | "fenced" (D2)
  "sentinel_min_interval_secs": 5, "sentinel_hourly_budget": 60,
  "sidecar_version": "…",
  "boot": { "holder_id": "…", "boot_unix": 1756… } }
```

Absence ⇒ old sidecar ⇒ **sentinels are live ammunition** (they'd be scanned and published, §2.0); agents must check first. **Rollback direction (review: crash-takeover):** a rollback to an old sidecar over a live emptyDir leaves a *stale* marker claiming protocol 1 while the old sidecar scans `.flint/` — the hazard with the safety catch painted green. The `boot` stamp is the tell: the agent contract treats a marker whose boot stamp has not changed across an observed sidecar restart as suspect, and the runbook names the downgrade hazard loudly. B14 covers both directions (marker-absent old image; stale-marker-after-downgrade).

**UDS door (Phase 5, opt-in):** a Unix socket at `.flint-sync/ctl.sock` (state dir — outside the scan by the shipped exclusion) serving `POST /v1/boundary`, `POST /v1/sync`, `GET /v1/status`. Implementation: thin translation onto the *same* consume queue as the file sentinels — one code path, the socket is sugar with a synchronous response instead of ack-polling. Pod-internal only; no TCP listener; consistent with the no-new-remote-surface non-goal.

**Gateway verb (Phase 5, opt-in):** `POST /lean/v1/{workspace}/boundary` (bearer-authed like all verbs, `gateway.rs:12-38`) lets HITL/CI request a boundary from outside. **Transport revised (review: security-dos):** the draft routed this as a fake no-object `InboxEntry`, but `consume_inbox` HEADs `file_key(path)` for every entry and a no-object entry lands in the NotFound arm (`barrier.rs:80-90`) as a spurious `consume-object-missing` conflict — and special-casing the single most safety-critical function in the crate is worse. Instead the inbox **document** gains a dedicated `boundary_request: {requested_unix, requestor}` field, set by the gateway under the same CAS discipline, consumed by explicit logic at the sidecar's next inbox read (which happens every barrier/lane tick already — promptness is one floor, **zero added polling**, no per-tick GETs beyond today's). A consumed boundary request is treated as a consumed publish sentinel: same min-interval and hourly budget, same ack path (the ack lands in `.flint/publish.ack` and the report in gateway `/status`). Window/409 semantics untouched; per-request epoch validation does not apply (it claims no epoch).

### 2.6 CRD / chart knobs, status, gauges

New `FlintLeanWorkspaceSpec` fields (today's fields: `crd.rs:35-95`; flow to the sidecar via `inject.rs`'s `FLINT_SYNC_*` env list, `inject.rs:40-54`):

| Knob | Default | Default == shipped behavior? | Meaning / trade stated in the CRD doc-comment |
|---|---|---|---|
| `boundaryMode` | `hybrid` | yes (sentinel-silent hybrid ≡ cadence) | `cadence` = pre-boundary behavior; `hybrid` = cadence ∪ sentinels (no trade); `gated` = **opt-in**: trades automatic-recovery RPO + copy requests + staging storage + proxy conformance for coherent views |
| `sentinels` | `auto` | yes | `auto` = verbs on unless pre-existing `.flint/` data detected (D0.4); `off`; `force` (accepts consumption of pre-existing sentinel-named files) |
| `sentinelMinIntervalSecs` | `5` | yes (inert without touches) | latency guard; sentinels inside the interval coalesce |
| `sentinelHourlyBudget` | `60` | yes (inert without touches) | request-lever cap (review: security-dos); exhausted ⇒ honors defer to the floor, ack stamped `sentinel-deferred` |
| `visibilityLagBoundSecs` | unset; **required iff gated** | n/a | hard cap on citation staleness; forced citation stamped `forced-lag-cap` in ack **and** manifest meta |
| `quiesceBoundSecs` | `30` (gated only) | n/a | quiescence = scan-to-scan stability window (review: gc-durability) |
| `stagedBacklogCapObjects` / `…Bytes` | `5000` / `2 GiB` (gated only) | n/a | forced-citation source bounding the drain (review: ops-fleet) |
| `perFilePublishCooldownSecs` | **`0` (= today: publish every tick)** | **yes — the draft's 300 default was an RPO regression for >64 MiB files and is corrected (review: ops-fleet)** | opt-in; applies to files > `whole_put_max` (64 MiB, `lib.rs:67-69`) in all modes — *builds* the lean-plan §2.1/§6 cooldown (verified absent from code); never applies to sentinel-honoring barriers' contents (D1 corollary) |
| `eagerOrphanAgeoutDays` | `3` (gated only) | n/a | staging-prefix lifecycle age; cross-validated ≥ 2×(lagBound+floor) (D8) |

`sentinelPollSecs` stays env-only (`FLINT_SYNC_SENTINEL_POLL_SECS`, default 1) — not a fleet contract.

**Propagation semantics — stated, because they bite (review: ops-fleet):** sidecar config is env stamped at pod creation by the mutating webhook (`inject.rs:108-131`); there is no re-read path. Changing any of these on a CR affects **new pods only**; the gated→cadence "escape hatch" therefore requires pod recreation — which destroys the emptyDir pending record and converts the whole uncited window into D9 orphans. That is why `recover-staged` is a v1 deliverable (D9) and why the CRD doc-comment for `boundaryMode` states the recreation requirement verbatim.

**Status (new, additive):** `FlintLeanWorkspaceStatus` (today `crd.rs:114-128`, no conditions array) gains `conditions[]`: `BoundaryModeAccepted` (spec validation: gated-without-lag-bound, failed ageout cross-validation, failed conformance probe, insufficient grace ⇒ False with the probe/rule named), `StagingLifecycleProvisioned` (operator-side D8 posture), `SentinelVerbsActive` (D0.4), and **`BoundaryModeActive`** (review: ops-fleet): the sidecar echoes `{sidecar_version, protocol, active_boundary_mode, last_cited_seq, last_cited_unix, staged_uncited_count}` into the lease-heartbeat cell it already writes (D12 renews it every ≤30 s — the echo is free); operator reconcile compares spec vs observed and sets `BoundaryModeActive=False` on mismatch. This closes the operator↔sidecar mixed-version hole the way D11 closes agent↔sidecar: an old sidecar binary ignoring `FLINT_SYNC_BOUNDARY_MODE=gated` (env read is a fixed list, `flint_sync.rs:56-60`) no longer runs fused cadence behind a green condition. It also gives the operator live per-workspace visibility lag as a condition/printer column with no metrics stack.

**Chart/CRD delivery corrected (review: ops-fleet):** the CRD is force-server-side-applied from the operator's **compiled-in copy** at startup (`flint_lean_operator.rs:129-148`); `crds/` in the chart is bootstrap-only. New knobs therefore require the operator binary; upgrade order is operator-first; and an operator **rollback force-applies the old schema, silently removing `boundaryMode`** — named in the upgrade notes. Gateway boundary verb ships behind the existing `gateway.enabled=false` block (`templates/gateway.yaml:1-8`).

**Gauges + fleet-minimum observability (review: ops-fleet — pulled into Phase 3, not Phase 6; gated mode introduces wedge shapes an operator must be able to page on before a metrics stack exists):**
- Sidecar writes `.flint-sync/gauges.json` atomically each tick: `{rpo_secs, visibility_lag_secs, staged_uncited_count, staged_uncited_bytes, staged_age_max_secs, withheld_reason, sentinel_budget_remaining, forced_citation_count, last_boundary: {source, seq, unix}}`. Agent- and exec-visible immediately.
- A structured per-tick stderr line carries `withheld_reason` (awaiting-boundary | parked-412 | cas-conflict | copy-probe-failed | quiesce-pending) — today's only signal surface is stderr (`flint_sync.rs:156-162`), so it must at least be greppable and structured.
- `flint-sync status` exec subcommand renders gauges + pending + lease state (diagnosis without spelunking the emptyDir).
- The lease-heartbeat echo (above) is the fleet-visible lag surface: operator printer columns `CITED-SEQ`, `LAG`, `STAGED`.
- Gateway `GET /status` gains `last_cited_seq`, `manifest_stamp_unix`, `boundary_source` (from the manifest HEAD it already can do).
- **Conformance probe cadence (review: ops-fleet):** the CopyObject/DeleteObjectVersion probe (§5 Phase 3 gate) runs at sidecar startup **and** on the operator's reconcile cadence — proxies upgrade; a once-at-Phase-3 probe is not a posture. Failure flips `BoundaryModeAccepted=False` with the probe named.
- Prometheus scrape plumbing (a `/metrics` listener or textfile-collector contract) is Phase 6 and an open question (§8 Q5) — not invented silently here.

---

## 3. What v1 deliberately does NOT solve (residuals — stated, not hidden)

1. **mtime-granularity boundary evasion.** A write that changes bytes without changing size or coarse mtime within the same second can evade the scan diff (`scan.rs:73-75`) and therefore the boundary guarantee. Same residual as the barrier's; the ack does not upgrade it.
2. **No fsync-derived boundaries.** No interception is a non-goal; the boundary-source enum reserves the name only.
3. **Gated automatic-recovery RPO is the last boundary, not the floor.** Revised (review: gc-durability/ops-fleet): staged bytes are durable until ageout, *named durably* in `orphans.json` (D9), and `recover-staged` can re-install them as a flagged boundary — but nothing automatic does; on pure-spot fleets pod replacement is routine and the CRD doc-comment says all of this verbatim, including: uncited = invisible to every import/DR/cross-cluster checkout.
4. **Deposed-straggler direct PUTs in cadence/hybrid modes** remain the 7,591-class exposure until proxy-side P5 lands (chaos finding 2). Gated mode narrows the class to `files/` data keys only — the shared inbox cell retains a TOCTOU exposure that cooperative re-reads shrink but cannot close (§2.4.2 property 2; review: crash-takeover).
5. **No Prometheus scrape path** — gauges are files + heartbeat echo + gateway JSON until Phase 6 (§8 Q5).
6. **Sentinel authorship is per-workspace, not per-agent — and acks are forgeable in-pod** (review: security-dos). Multiple processes share one sentinel pair; nonces + cumulative covered-nonce acks are the disambiguation. The ack is advisory (§1.2); the authoritative durability read is remote.
7. **`.flint/publish` has no scope** — whole-workspace boundaries only.
8. **UDS/gateway doors are optional and late** (Phase 5); the file protocol is the only guaranteed interface.
9. **Legacy `files/.flint/...` citations are frozen, not migrated** (D0.2) — a warning record, no auto-rewrite.
10. **CopyObject/DeleteObjectVersion conformance of the deployment proxy is a gate, not an assumption** (§5 Phase 3 gate; §8 Q2) — re-probed on the operator cadence, never assumed stable.
11. **The citation copy-phase transient reader window** (review: gc-durability, model-drill). Between the first CopyObject and the CAS, a concurrent reader (HITL GET, sibling-cluster sync, a checkout let in by claim timing) can observe boundary-new bytes under old citations and S3-wins-adopt a mixture. The crash/permanent-half-boundary case is closed by the citation-intent roll-forward (D7); the transient window remains, scales with boundary size, and is stated here rather than hidden. Structural closure = versioned staging (§8 Q2, reopened on exactly this ground).
12. **Sentinel request amplification is bounded, not eliminated** (review: security-dos). An agent at the budget ceiling still costs more requests than cadence-only; §7 prices the ceiling, per workspace and per project proxy.

---

## 4. Formal model tranche 3 (rewritten — review: model-drill)

Extend `lean/formal/LeanSubtree.tla` by the tranche-2 pattern: new CONSTANTS default-FALSE in every existing cfg so tranche-1/2 state spaces are preserved by construction (`LeanSubtree.tla:40-64`, `gen-cfgs.sh` emit; bump `check.sh`'s `$PASS` and the README count). New constants: `BoundaryEnabled`, `GatedCitation`, `EagerStaging`, `SyncScope`.

**Substrate — the draft's "no new substrate" claim was false and is retracted (review: model-drill, critical).** In the shipped spec, `objects[p]` *is* `files/<path>`: GCDelete deletes it, `Inv_NoDangling` checks manifest→objects, CheckoutB reads it. Modeling an eager upload as `objects[p]` advance would model the **in-place overwrite D7 explicitly rejects** — verifying the refused design (the thrice-paid "the abstraction was the bug" class, committed in the planning document). Real substrate additions:

- `staged` — a per-path map of the one staged generation (latest-wins collapse of per-uuid stage keys; safe because per-key guards are independent and the lane deletes superseded generations — the collapse is *stated* in the abstraction ledger).
- `Copy` — the citation action moving `staged[p]` into `objects[p]` under the If-Match guard, distinct from `Upload`.
- `pending` — a component of sidecar state that survives `Restart` but dies with pod-replacement `CrashPod`, carrying `base_etag` per path (the D7 re-validation guard).
- `sentinel` token + ack ghost (`gh`) for product 1; `citation_intent` ghost for roll-forward.
- A **Reader** action observing `(objects, manifest)` between `Copy` and `CASInstall` (review: model-drill — the shipped model deliberately abstracts checkout's 412/S3-wins arm, safe at one-tick payloads, no longer safe when the tranche's whole point is boundary-sized payloads). Invariant: no reader materializes post-boundary bytes under pre-boundary citations except through the roll-forward path.
- Abstraction ledger entries for everything still collapsed (per-uuid stage keys → one slot; ack file → ghost; budget/min-interval → out of the safety gate).

**Products, invariants, mutations, probes** (probes are REQUIRED-violated per the house rule — products 2–4 each carry them; the draft named probes only for product 1, leaving strict runs free to hold vacuously):

1. **boundary × barrier × inbox** — sentinel consume/honor/ack interleaved with consume_inbox, window, CAS, crashes at every point, **and deposal** (an `AllowStall` cfg variant — the draft's cfgs never checked `Inv_AckImpliesCited` across a fence; review: model-drill). Invariants: `Inv_AckImpliesCited` (an ok-ack naming seq S ⇒ the manifest at S cites every pre-boundary write), `Inv_AckNotEarly`, `Inv_NoNonceOrphan` (every consumed nonce is eventually named by exactly one ack, ok or refused), `Inv_RefusedNeverInstalled` (a refused ack coexists with no install at our epoch after the fence). Mutations: skip pending-rename (duplicate honor); ack-before-CAS; drop pending-defeats-fast-path; **ack-from-stale-baseline** (the retracted crash-matrix shortcut — must violate `Inv_AckImpliesCited`); **consume-over-pending** (must violate `Inv_NoNonceOrphan`); success-ack-after-fence. Probes: `ProbeSentinelHonored`, `ProbeAckAfterCrash`, `ProbeRefusedAck`.
2. **gated-citation × GC × ageout** — pending sets, withheld deletes, GC ordering, lifecycle reap racing a live pending record, citation-intent roll-forward. Invariants: `Inv_NoUncitedGC`, `Inv_BoundaryAtomic` (mutations: split install across two CASes; **successor ignores citation-intent** — both must fire), `Inv_NoResurrection` under withheld deletes, `Inv_ManifestKeysUnderFiles` (mutation: cite-in-place — must fire), `Inv_HITLDurable` under the **stale-base-etag copy** mutation (D7 re-validation), stage-NotFound arm reachable (ageout action). GC-set-from-INSTALLED-manifest stays model-pinned; its mutation must still fire with gating on. Probes: `ProbeCitationInstalled` (a pass installs ≥2 paths staged in *different* lane passes), `ProbeWithheldDelete` (withheld then installed), `ProbeLagCapForced`, `ProbeGC` re-run with `GatedCitation=TRUE`.
3. **eager-upload × takeover** — staging PUTs from a deposed straggler racing rotation and a successor's citation, **including the shared inbox cell** (review: crash-takeover): the straggler's `drop_entries`/`open_window` against the successor's inbox is a named interleaving. Invariants: `Inv_NoDeposedPut` restricted to `files/` holds with `EpochCheck` off; successor never copies a foreign-epoch stage entry; mutation: citation copies a stale-epoch stage — must violate `Inv_NoStragglerInstall`; mutation: straggler drops successor-queued inbox entries unfenced — must fire against `Inv_HITLDurable`. Probe: `ProbeStragglerStagePut` (a deposed writer's staging PUT actually lands — containment must never be proven by the attack's absence).
4. **scoped-sync × gated-citation × inbox** (new — review: model-drill; D4 rewrites the merge base the model refuted naive designs on twice, and gets modeled before code per the FlintTierSession precedent). Mutations: scoped sync advances whole `inst_base` → out-of-scope foreign entry lost (must fire); repair-only pass installs withheld tombstones → torn rename (must fire against the pinned repair-only invariant). Probe: `ProbeScopedSyncApplied` + `ProbeOutOfScopeLater` (the deferred foreign change integrates at a *later* consume).
5. **Liveness / rate limiting stays OUT of the safety gate** — fairness, per the house rule (`LeanNoWindowHolds`; `README.md:47-53`). D12's renewal is drilled (B17), not modeled.

**Budget table + affordability (review: model-drill — `gen-cfgs.sh:42` records the measured wall: "MaxBarriers=3 + MaxGen=4 blows past an hour"; the breadth gate runs MaxGen=3/MaxBarriers=2, and product 2's minimal interesting trace needs ≥3 lane/citation passes — the naive extension does not fit).** A separate `MaxLanePasses` budget constant keeps upload ticks from drawing `MaxBarriers`. Per-cfg budgets (mirroring the takeover cfgs' MaxHitl=0/MaxCrashes=0 depth-buying trick):

| cfg | arms off | budget |
|---|---|---|
| P1 breadth | Gated off | MaxGen=3 MaxBarriers=2 MaxSentinels=2 crashes on |
| P1 deposal | AllowStall=TRUE, MaxHitl=0 | MaxGen=3 MaxBarriers=2 MaxSentinels=1 |
| P2 gated×GC | MaxHitl=0 MaxCrashes=1 | MaxGen=3 MaxLanePasses=3 MaxBarriers=1 |
| P2 roll-forward | MaxHitl=0 | MaxGen=2 MaxLanePasses=2 MaxCrashes=1(+replace) |
| P3 straggler | AllowStall=TRUE EpochCheck=FALSE | MaxGen=2 MaxLanePasses=2 MaxHitl=1 |
| P4 scope | MaxCrashes=0 | MaxGen=3 MaxSyncs=2 MaxLanePasses=2 MaxHitl=1 |

**Pilot obligation:** the P2 breadth cfg must complete under 30 minutes on the gate hardware before Phase 3 is declared gated. Fallback on overrun: shrink via a *stated* abstraction-ledger entry — never by silently lowering any depth below its mutation's counterexample length (the quiet failure this table exists to prevent is strict runs going green without ever reaching a citation pass).

**Gate:** tranche-3 cfgs green (strict + named-violation mutations + violated probes) **before Phase 3 code merges** — the FlintTierSession precedent (model refuted the naive design before code) is the reason gating precedes implementation.

---

## 5. Phases, gates, acceptance criteria

Sentinels ship before gated advance — Phases 0–2 are independently shippable and default-inert.

**Red-first discipline (review: model-drill), applying to every gate below:** every new test and drill leg must be **observed red** against pre-phase code or with the new guard disabled — and where the literal test cannot run on old code, the honest red form is named in the gate. For D0 that form is: a test asserting `.flint/publish` is NOT published must **fail today** (`scan.rs` skips only `.flint-sync` — the hazard demonstrated as a failing assertion before D0 lands).

**Phase 0 — namespace reservation + capability marker + pre-flight (D0, D11).**
Scan exclusion, delete-classification exemption, checkout guard, pre-existing-`.flint/` detection + `SentinelVerbsActive` condition, `capabilities.json` written at every run startup (with boot stamp).
*Gate:* full battery green + new tests: `flint_dir_never_scanned` (red form above), `legacy_flint_citation_survives_upgrade` (backdated baseline with a `files/.flint/x` entry → two barriers → still cited, warning record present), `capabilities_written_on_live_tree_restart` (the marker-present row — review: crash-takeover), `preexisting_flint_disables_sentinels`. Drill legs B13, B22.
*Acceptance:* a workspace that never touches `.flint/` shows request-count-identical bucket traffic before/after — measured as **delta vs a recorded control whose request shape is written into the leg** (the real idle tick is 4 requests — renew CAS + epoch read + inbox GET + manifest HEAD — not 1 HEAD; review: ops-fleet), and the control run must show a nonzero, category-correct baseline before the differential counts (a dead observer must not pass the leg — the runaq/runar class).

**Phase 1 — publish sentinel + ack + rate limit + renewal discipline (D1–D3, D12), hybrid semantics.**
Interval-based loop rewrite, consume/honor/ack/retire with settle-before-consume, refused-fenced acks, torn-body rule, type checks, covered-nonce acks, min-interval + hourly budget.
*Gate:* tranche-3 product 1 modeled and green FIRST (including the deposal arm); unit tests `sentinel_ack_echoes_covered_nonces`, `crash_between_consume_and_ack_reruns_barrier` (MemoryStore + pending-file injection — asserts the ack seq is the re-run barrier's install, never persisted state), `restart_settles_pending_before_new_consume`, `torn_pending_body_honored_as_bare_touch`, `fenced_honor_writes_refused_ack`, `fifo_sentinel_skipped`, `budget_exhaustion_defers_to_floor`; drill legs B1–B4, B8, B16, B17, B18.
*Acceptance:* on the 1-hour-floor fixture, sentinel→ack ≤ 10 s for a 100-file dirty set; storm of 100 touches/10 s produces ≤ `ceil(10/minInterval)+1` barriers and the final ack's nonce set covers every touch; idle workspace with marker present and agent silent adds zero bucket requests beyond the recorded control shape (D12's 30 s renewal is *in* the control shape and priced in §7); B17 shows no takeover under storm.

**Phase 2 — sync sentinel + scope (D4) + remote.seq (D5) + write containment.**
*Gate:* unit tests `scoped_sync_preserves_out_of_scope_foreign_flow` (the D4 hazard test — out-of-scope change applied by a LATER consume, not lost), `sync_ack_carries_conflicts`, `sync_rehonor_no_phantom_conflicts` (crc64 identity — review: crash-takeover), `write_file_atomic_refuses_symlink_escape` + `scope_matches_on_component_boundary` (review: security-dos — red against shipped `write_file_atomic`), `sync_honor_refused_when_fenced`; **product 4 modeled and green before D4 code merges** (review: model-drill); drill legs B5–B7.
*Acceptance:* B5's out-of-scope integration observed; remote.seq `updated_unix` heartbeats every HEAD tick and `observed_seq` moves within one floor of a foreign install with zero added bucket requests (request-count oracle vs control).

**Phase 3 — gated advance (D6–D10).**
Staging lane (no window, baseline∪pending diff, superseded-key deletes, ageout refresh), `pending.json`, quiescence (scan-to-scan) / lag-cap / backlog-cap sources, copy-cite pass (base-etag re-validation, pinned AdoptOwn recognizer, NotFound arm, citation-intent + roll-forward, post-CAS stage deletes), withheld deletes + repair-only tombstone withholding, boundary-source manifest stamp, preStop settle/retry/sizing, takeover surfacing + durable orphans.json, Phase-3 observability minimum (gauges fields, withheld_reason line, heartbeat echo, `flint-sync status`).
*Gate:* tranche-3 products 2 + 3 green BEFORE merge, **including the P2 affordability pilot (§4)**; **proxy conformance probe extended**: (i) conditional-header preservation, (ii) stale-epoch refusal readiness, (iii) `CopyObject` with metadata + If-Match semantics intact, (iv) `DeleteObjectVersion` permitted on versioned buckets (review: gc-durability) — a failing probe blocks gated mode for that fleet (`BoundaryModeAccepted=False`, message names the probe; re-probed at startup + operator cadence). Battery additions per invariant, including `hitl_put_admitted_between_citations` (review: crash-takeover/security-dos) and `clear_intent_keys_preserves_pending`. Drill legs B9–B12, B15, B19, B20, B21.
*Acceptance:* B9 mid-logical-change invisibility observed with a *completed* probe checkout; B10 forced citation ≤ `visibilityLagBoundSecs`+one floor with the source readable from the bucket; B11 lossless preStop incl. SIGKILL and SIGTERM-ignoring variants; B12 straggler containment with the inbox-cell oracle; B15 roll-forward; B20 quiescence actually fires; B21 staging returns to ~0 and versioned ageout reclaims.

**Phase 4 — CRD/chart/operator (§2.6) + recovery tool.**
Knobs, validation (gated⇒lag-bound; ageout cross-validation; grace sufficiency), conditions (incl. `BoundaryModeActive` from the heartbeat echo, staging-without-lease event), staging lifecycle provisioning/sweep with versioned rules + read-merge-append (D8), webhook grace derivation (D10), `flint-sync recover-staged` (D9 — v1, review: ops-fleet/gc-durability).
*Gate:* kind e2e extended: a CR with `boundaryMode: gated` and no lag bound is Refused at reconcile (failing control, house style); ageout cross-validation refused; spec/observed mode mismatch flips `BoundaryModeActive` (old-sidecar-image control leg); chart install green with new values; recover-staged installs a named set as a flagged boundary on a rig with manufactured orphans.
*Acceptance:* `StagingLifecycleProvisioned` True on a MinIO rig with versioning ON (noncurrent rules verified), sweep-mode fallback exercised with version-scoped deletes.

**Phase 5 — layered doors (§2.5), opt-in.** UDS socket; gateway boundary verb via the dedicated inbox-doc field.
*Gate:* gateway verb drill leg (boundary request lands as a consumed sentinel via the `boundary_request` field — no conflict records minted; window-409 path exercised; min-interval + budget honored); UDS and file sentinel proven to share one consume path (a UDS boundary and a file sentinel in the same interval coalesce into one barrier, one covered-nonce ack).

**Phase 6 — observability plumbing.** Pending §8 Q5 (the Phase-3 minimum already ships lag, reasons, and heartbeat echo).

---

## 6. Drill matrix

House rules inherited wholesale (`docs/plans/flint-lean-chaos-drill.md:33-61`, `lean/e2e/run-chaos.sh`): `leg()` harness with PASS/FAILED + NOTES, per-leg prefix isolation (`tenants/bN`), mc oracle reading the bucket directly, zero-padded filenames so lexicographic upload order makes mid-flight observable by two HEADs, `wait_key` never sleep-and-hope, and **every leg observes its own precondition or FAILS** — the first chaos run scored 4/10 on exactly this. The falsifiability fixture for boundary legs is the cluster drill's 1-hour-floor trick: with `floor_secs=3600`, only the mechanism under test can advance the manifest. Every leg is first observed red per §5's discipline.

| Leg | What it proves | Anti-vacuity guard (the leg FAILS if unobserved) |
|---|---|---|
| B1 | Sentinel publishes when cadence cannot: 1 h floor, dirty files, touch `.flint/publish` → manifest advances, ack covers nonce | Control phase first: assert the manifest does NOT advance for 3 min without the sentinel — proves the floor fixture actually removed cadence from the leg |
| B2 | Crash between consume and ack converges via the uniform re-run rule: SIGSTOP mid-honor (two-HEAD mid-flight check), SIGKILL, restart → full barrier re-runs, ack carries the re-run's seq | Assert `publish.pending.json` exists AND `publish.ack` absent (or stale-nonced) *before* the kill; assert post-restart ack seq == the re-run barrier's installed seq, not the pre-crash baseline seq (review: crash-takeover) |
| B3 | Rate limit + coalescing: 100 touches in 10 s ⇒ ≤ `ceil(10/5)+1` barriers; final ack's `nonces[]` covers EVERY touched nonce (review: security-dos) | Assert touch timestamps actually beat `sentinelMinIntervalSecs`; assert a mid-storm nonce (not the last) appears in the covered set |
| B4 | Stale-ack discrimination: pre-seed an old ack, fresh touch → **fresh ack arrives** with echoed nonce and covering mtime; agent-side check distinguishes both | Assert the stale ack exists with an older nonce BEFORE the touch AND the fresh ack lands (review: model-drill — discrimination with nothing new to find is vacuous) |
| B5 | Scoped sync: foreign changes under `inputs/` and `outputs/`, scope=`inputs/` ⇒ only `inputs/` applied now; `outputs/` integrates via the NEXT barrier's inbox flow (the D4 rule) | Assert the `outputs/` foreign change existed pre-sync AND was absent at sync-ack time AND present after the next barrier — all three |
| B6 | Conflict transport: dirty local file vs foreign change ⇒ `sync.ack.conflicts[]` non-empty, local bytes untouched | Assert local dirt observed (scan-visible mtime/size delta) before the sentinel |
| B7 | remote.seq is local-only news: foreign install advances manifest; a probe container — no creds, no network — polls only `.flint/remote.seq` until `observed_seq` moves; `updated_unix` heartbeats meanwhile | Assert `observed_seq` had NOT moved before the foreign install; assert the probe container's only access is the file; assert `updated_unix` advanced across an idle tick (review: ops-fleet) |
| B8 | Zero-added-cost when unused: marker present, agent silent for N floors | Oracle counts requests per idle tick **as a delta vs a recorded control whose 4-request shape (renew CAS, epoch read, inbox GET, manifest HEAD) is written into the leg** (review: ops-fleet); assert no `files/.flint/*` key ever appears; assert no ack files appear |
| B9 | Gated invisibility of mid-logical-change: write A then B, boundary only after B; a second pod checkouts between A's stage and the boundary — **the probe checkout must COMPLETE and materialize exactly the pre-boundary cited set** (file count + etags vs a recorded manifest snapshot) (review: model-drill) | Assert (two-HEAD trick) A's staged object EXISTS under `.flint/lean/eager/` while manifest seq is unchanged — durability observed, visibility withheld — before the probe runs; a wedged/errored probe FAILS the leg |
| B10 | Lag cap forces citation: gated, no sentinel, writer keeps the tree non-quiescent ⇒ citation by `visibilityLagBoundSecs`; source `forced-lag-cap` readable **from the bucket manifest meta alone** (review: ops-fleet) | Assert the writer actually wrote during every quiesce window (post-hoc mtime audit) — else quiescence fired and the leg proved nothing about the cap |
| B11 | preStop cites everything: (a) gated SIGTERM ⇒ final manifest cites all, staging superseded, owed ack settled; (b) SIGKILL mid-drain + pod replacement ⇒ recovery at last boundary, orphans surfaced in durable `orphans.json` (review: crash-takeover); (c) SIGTERM-ignoring agent ⇒ citation still lands within derived grace or loss equals the documented bound (review: gc-durability) | Assert `staged_uncited_count > 0` (gauges + staging LIST) immediately before SIGTERM — a drain leg with nothing to drain is vacuous; (b) asserts the pending record genuinely died with the emptyDir |
| B12 | Straggler containment: SIGSTOP sidecar, force takeover, thaw ⇒ straggler's eager PUTs land only in ITS staging prefix; `files/` etags unchanged; **inbox doc snapshot before freeze vs after thaw: no entry disappeared, no stale-epoch window appeared** (review: crash-takeover); successor surfaces orphan candidates | Assert the thawed straggler's PUT count > 0 via oracle (the C3 pattern — 7,591 was measured, not assumed) AND record pre/post etags of every `files/` key |
| B13 | Legacy `.flint/` upgrade safety: pre-seed a cited `files/.flint/legacy.txt`, upgrade sidecar, two barriers ⇒ citation survives, warning record present | Assert the legacy citation existed in manifest AND bucket pre-upgrade |
| B14 | Mixed-fleet detection, both directions: (a) old sidecar image (pinned pre-D0 tag, named in the leg — review: model-drill), marker genuinely absent ⇒ agent library refuses sentinels; recorded NOTE: the destructive control (touching anyway publishes `files/.flint/publish`, isolated prefix); (b) downgrade-over-live-tree: stale marker present, old image ⇒ agent's boot-stamp check flags it suspect (review: crash-takeover) | (a) assert marker absent + image version verified; (b) assert the stale marker's boot stamp is unchanged across the observed restart |
| B15 | Citation-pass crash + roll-forward (review: gc-durability, model-drill): SIGSTOP mid-copy (two-HEAD: first copied key at new etag, manifest seq unchanged), kill + **pod replacement**; successor finds citation-intent, rolls the citation FORWARD; a concurrent probe checkout observes a coherent view | Assert citation-intent existed in the bucket before the kill AND ≥1 copy had landed AND the pending record died with the emptyDir; probe checkout must complete (B9 rule) |
| B16 | Hybrid default-floor regression (review: model-drill): dirty tree, NO sentinel, default floor ⇒ manifest advances within floor+slack — the rewritten select/interval loop did not starve cadence | Assert no sentinel file ever existed during the leg (else it tested the wrong arm) |
| B17 | Renewal under storm (review: crash-takeover, security-dos): floor 3600, standby claimant pod, continuous sentinel storm ⇒ NO takeover across N minutes; heartbeat renewals observed at ≤30 s cadence | Assert the standby really observed the lease cell (its quiet-poll log advances) — a dead standby proves nothing |
| B18 | Deposed mid-pending ⇒ refused ack (review: crash-takeover): force takeover while a pending sentinel stands; thaw ⇒ `publish.ack` with `status:"refused-fenced"` naming the epoch; capabilities flips to fenced; a subsequent sync sentinel on the zombie is also refused, tree unmutated | Assert the pending file existed at deposal AND the successor's manifest advanced (takeover real) AND the zombie's tree hash is unchanged after the refused sync |
| B19 | HITL admitted between citations (review: crash-takeover, security-dos): gated, upload lane ticking, no citation due ⇒ gateway HITL PUT succeeds (no 409) in every inter-citation interval | Assert lane ticks actually ran during the interval (stage keys appeared) — else no window could have been wrongly opened |
| B20 | Quiescence fires (review: gc-durability): gated, ONE write then silence ⇒ citation at ~`quiesceBoundSecs` with `boundary:"quiescence"` | FAIL if the source reads `forced-lag-cap` — the exact dead-code shape the finding named |
| B21 | Staging reclamation (review: gc-durability): steady-state churn ⇒ staging prefix object count returns to ~0 after each citation (superseded + post-CAS deletes); versioned-MinIO variant: after ageout, **version count** (not just LIST) reaches 0 | Assert the prefix was non-trivially populated mid-leg (count > N) before asserting it drains |
| B22 | Pre-existing `.flint/` data (review: ops-fleet): workspace with app-owned `.flint/publish` file, new sidecar ⇒ sentinels disabled (`SentinelVerbsActive=False`), the file NOT consumed, NOT published, still present and byte-identical | Assert the file existed pre-upgrade with recorded bytes; assert `capabilities.json` reports `verbs: []` with the pre-flight reason |

Total: 22 legs; register with an "of 22" count to bump, rig reset re-runnable, per the shipped harness.

---

## 7. Economics (rebuilt in request units — review: ops-fleet, security-dos)

Baseline prices (S3 standard): PUT/COPY/CAS $0.005/1k, HEAD/GET $0.0004/1k; intra-region copy moves no billable bandwidth; DELETEs and lifecycle expirations are free.

**The real idle tick (correcting the draft):** 4 requests, not 1 HEAD — lease renew (a CAS write, **PUT-priced**, `flint_sync.rs:154`), `verify_not_deposed`'s epoch read, `consume_inbox`'s inbox GET (runs *before* the fast path, `barrier.rs:190-193`), and the manifest HEAD. Fleet baseline at 3,000 workspaces / 60 s floor: 50 PUT/s + 150 GET/s ≈ **$27/day** — that is *today's* cost, unchanged by this plan, but the draft's "$0.06/hour, 1 HEAD" figure was ~19× under (renewals are PUT-priced) and B8's old oracle would have failed against a correct sidecar on day one.

**This plan's default-posture delta:** D12's 30 s heartbeat renewal adds +1 PUT/min/workspace when floor > 30 s ⇒ +50 PUT/s fleet-wide ≈ **+$21.6/day at 3,000 workspaces** — the one deliberate default increase, bought for takeover-safety (§2.1a), stated here and in the D12 doc-comment. remote.seq, the sentinel poll (2 local lstats/s), and the capability marker add **zero** bucket requests.

**Hybrid (default):** published *bytes* unchanged — a sentinel barrier publishes the same dirty set earlier. Requests: each forced barrier ≈ 3 PUT-priced (window CAS, manifest CAS, renew) + 3 GET-priced. Uncapped worst case at min-interval 5 s would be 17,280 forced barriers/day ≈ $0.28/day/workspace control-plane **plus** the real cost the draft missed: re-PUTs of hot small files at 12× cadence, agent-controlled (a 1-byte file dirtied in a loop re-PUTs every 5 s — request-priced identically to a large PUT). `sentinelHourlyBudget=60` caps sustained forced barriers at ≈ cadence-equivalent (≤1,440/day): sustained worst case ≈ 2× today's barrier count per storming workspace ≈ +$0.03/day/workspace control-plane + one extra re-PUT per dirty file per honored sentinel. Fleet-correlated exposure (review: ops-fleet): the budget is per-workspace; one framework bug touching `publish` in a loop *everywhere* multiplies by fleet size against the shared project proxy — per-project proxies should be sized for `workspaces × (budget+floor)/hour` barrier rate, and the D8/operator posture section carries that sizing note. Typical agent behavior (a boundary per logical op, minutes apart) is noise.

**Gated:** bytes at cadence (the lane diffs against baseline ∪ pending — §2.4.1 — so per-file PUT count per tick cannot exceed today's; hot >64 MiB files additionally pay per `perFilePublishCooldownSecs` only if that opt-in knob is set). Added: **one COPY per dirty file per citation** (request-priced as PUT, no bandwidth) + staging storage for at most one generation per dirty path (superseded-key deletes make the churn term vanish; without them staging would scale as churn × ageout — the reviewed 42 GiB/file failure — which is why the deletes are mandatory, not an optimization). Per-workspace example: citing every 5 min with 50 dirty files = 14,400 COPY/day ≈ $0.07/day. **Fleet column (review: ops-fleet):** 3,000 such workspaces = 43.2M COPY/day ≈ **$216/day ≈ $6.5k/month**, and ~500 sustained req/s through the project proxies — gated adoption at scale is a proxy-QPS sizing exercise, stated in the CRD doc-comment. Orphan cost is bounded by `eagerOrphanAgeoutDays` of *crash-window* bytes only. Files >5 GiB use multipart `UploadPartCopy` in the citation pass — part-count-priced, same shape as the shipped compose path (`barrier.rs:479-562`).

**What the design refuses to spend:** no per-agent polling against the bucket (remote.seq exists so N agents cost 0 requests, not N×HEAD), no manifest GETs for news (HEAD stamp only), no added inbox polling for the gateway verb (rides the existing per-barrier inbox GET), no bucket-wide LISTs (staging list is prefix-scoped, claim-time and operator-cadence only), no unbounded forced-barrier rate (budget).

---

## 7b. Axis impact — the no-regression argument

**Under defaults (hybrid, sentinels present but unused unless touched):**

- **Consistency — improved, never traded.** The merge/park/conflict machinery is untouched (`manifest.rs:108-146`, upload 412 policy, sync scan-first rules). Hybrid adds coherent points to today's cadence points and removes none; citation timing never lags today's (B16 is the regression proof). D0's delete-exemption removes a latent upgrade data-loss hazard. D4 is designed *specifically* so a new verb cannot lose out-of-scope foreign flow — and is modeled before code.
- **Performance — zero *bucket* cost unused except the priced D12 heartbeat (review: ops-fleet — the draft's "zero-cost, 1-HEAD tick" claim was wrong at the baseline).** Scan skips `.flint/` (marginally cheaper); 2 lstats/s poll; remote.seq and marker are local renames; the delta vs today is exactly +1 renew PUT/min/workspace (§7), stated rather than hidden. B8 is the measured proof obligation, against the real 4-request control shape.
- **Security — surface unchanged by default; one class narrowed; two hazards closed that the verbs would have weaponized (review: security-dos).** No new remote surface for the app (file protocol only by default; UDS pod-internal opt-in; gateway verb bearer-authed and off by default). Sentinels are writable only by containers already sharing the mount — and the ack is documented advisory for the same reason (§1.2). The sync sentinel forced the pre-existing `write_file_atomic` symlink-traversal exposure to be closed (Phase 2) rather than made agent-triggerable. Sentinel type-checks close the FIFO/dir DoS. In gated mode the deposed-straggler blast radius shrinks from cited `files/` keys to a lifecycle-cleaned staging prefix — scoped honestly to data keys, with the inbox-cell TOCTOU named (§2.4.2); P5-at-the-proxy remains the plan of record for the rest.
- **Day-2 operations — additive, with the mixed-version and propagation holes closed (review: ops-fleet).** All knobs default to today's behavior (the draft's cooldown default violated this and was corrected to 0); conditions are new (none existed); the capability marker + heartbeat echo make **both** mixed-fleet directions (agent↔sidecar, operator↔sidecar) detectable instead of silently hazardous; knob propagation semantics (pod-recreate-only) are stated; the CRD-in-operator-binary delivery and rollback schema-reversion hazard are named; Phase 3 ships a pageable minimum (withheld_reason, heartbeat lag echo, `flint-sync status`) so "why is the manifest not advancing" is answerable before Phase 6.
- **Availability — degraded-to-today, never worse.** Sentinel consumption failure degrades to cadence publishing — hybrid's OR guarantees the manifest still advances on the floor, on an interval timer that no arm can starve (D3/D12 — the draft's select structure would have starved renewal and *deposed the writer*; closed). remote.seq fails stale-never-wrong with a heartbeated `updated_unix` as the tell. Fenced sidecars now answer their agents (refused acks) instead of stranding them. The gateway/proxy failure split (chaos C8/C12) is unmoved. The flock, lease-claim, takeover, and rotation protocols are untouched; renewal cadence tightens (safer, priced).
- **Multicluster — unmoved.** One-writer-per-subtree intact; boundaries are properties of the single writer's manifest sequence; remote.seq is per-pod local; the boundary-source stamp travels in manifest meta all clusters already read; nothing else crosses cluster boundaries that didn't already.

**What each opt-in trades (stated here and in the CRD doc-comments):**

- **`boundaryMode: gated`** trades: (1) automatic-recovery RPO — a replaced pod recovers to the last *boundary*; on pure-spot fleets replacement is **routine**, which is why `recover-staged` ships in v1 and the doc-comment states the spot-reclaim consequence and the DR corollary (uncited = invisible to every import/DR checkout) verbatim (review: ops-fleet, gc-durability); (2) request count — one COPY per dirty file per citation, with the fleet/proxy-QPS numbers in §7; (3) staging storage (one generation per dirty path + crash remnants); (4) proxy conformance requirements (CopyObject + DeleteObjectVersion), refused visibly via condition and re-probed on cadence; (5) a transient copy-phase reader window (§3.11). It buys: coherent views at every citation, straggler containment on data keys, and a bucket-readable coherence stamp.
- **Low `sentinelMinIntervalSecs` / high `sentinelHourlyBudget`** trade S3 request cost for boundary latency — bounded and priced in §7, per-workspace and per-proxy.
- **`perFilePublishCooldownSecs` > 0** trades >64 MiB-file RPO (floor → cooldown) for PUT cost — off by default (review: ops-fleet).
- **UDS door** trades one pod-internal listener for synchronous responses.
- **Gateway boundary verb** trades one more bearer-authed verb for HITL-initiated boundaries.
- **`cadence` mode** pins the exact pre-boundary behavior (the escape hatch) — reachable for new pods only; switching a live gated workspace manufactures orphans, which `recover-staged` exists to re-install.

---

## 8. Open questions for the user

1. **DR ageout posture (D8/D9).** During recovery from cluster loss, the 3-day staging lifecycle clock keeps running against the only copy of post-boundary work. Options: (a) runbook-only ("suspend/raise the eager lifecycle rule during DR" — requires bucket-admin action mid-incident), (b) the operator auto-suspends the rule whenever the staging prefix is non-empty with no live lease (automatic, but an operator outage then leaves rules suspended). Which posture?
2. **CopyObject-staging vs versioned-bucket staging — reopened and elevated (review: model-drill, gc-durability).** D7's copy design works on any conditional-write-conformant store, but carries the transient copy-phase reader window (§3.11) and the per-citation COPY bill ($6.5k/mo at the §7 fleet example). The versioned-bucket alternative — in-place PUTs with `versionId`-carrying manifest entries — closes the torn-reader window **structurally**, halves request count, but demands versioning conformance from every proxy and a `LeanEntry` schema change. The review found the fleet-recommended posture is versioning=Enabled already (`s3.rs:418-433`). Confirm the copy design for v1, or price the versioned bet now?
3. **Defaults check:** `sentinelMinIntervalSecs=5`, `sentinelHourlyBudget=60`, `quiesceBoundSecs=30`, `stagedBacklogCapObjects=5000`/`2 GiB`, `eagerOrphanAgeoutDays=3` (cross-validated ≥ 2×(lagBound+floor)), `perFilePublishCooldownSecs=0` (the 300 s draft default was a regression and is gone). Any of these contradict fleet expectations?
4. **Gateway boundary verb scope (Phase 5):** should the `boundary_request` field also support *sync* requests (HITL asks the sidecar to pull)? Sync-by-inbox mutates the live tree on a remote's say-so — arguably violating "agent-chosen safe point" — so the proposal is boundary-only. Confirm.
5. **Metrics plumbing (Phase 6):** localhost `/metrics` on the sidecar (new listener, pod-internal) vs textfile-collector contract on `gauges.json` vs gateway-only aggregation. The Phase-3 heartbeat echo already gives the operator per-workspace lag; this decides fleet-scrapeability. Preference?
6. **Cooldown home:** `perFilePublishCooldownSecs` is specified here (building lean-plan §2.1, now default-0). If you'd rather it land in the lean plan's own flush-profile tranche and be *referenced* here, Phase 3 takes the dependency instead of the implementation — say which.

---

## 9. Review ledger

43 findings, five lenses. FOLDED = integrated (section named); REJECTED carries the reason; overlapping findings are folded once and cross-referenced.

**crash-takeover**
- CT-1 (critical) Deposal strands sentinels; no REFUSED ack; zombie sync unfenced — FOLDED: D2 refused-fenced acks + fenced capabilities/gauges, sync honor gated on `verify_not_deposed`, model product 1 deposal arm, leg B18.
- CT-2 (critical) Crash-matrix ack shortcut signs unearned acks; consume clobbers surviving pending — FOLDED: uniform always-re-run-barrier rule + settle-before-consume (D2), TLC mutations `Inv_AckImpliesCited`/`Inv_NoNonceOrphan`, B2 oracle tightened.
- CT-3 (major) Gated upload lane opens the HITL window every tick, never clears — FOLDED: lane opens no window; window is citation-lane-only (§2.4.1); test + leg B19. (Same defect as SD-5, GD-8b.)
- CT-4 (major) Sentinel/citation barriers never renew the lease; long-floor fixture invites live-writer deposal — FOLDED: D12 renewal discipline (renew-before-every-barrier + 30 s heartbeat interval), leg B17.
- CT-5 (major) Straggler containment overclaimed — shared inbox/window cell still writable — FOLDED: property 2 scoped to `files/` data keys, epoch-cell re-reads, cell-compared window ops, B12 inbox-snapshot oracle, product 3 interleaving (§2.4.2).
- CT-6 (major) capabilities.json never written on live-tree restart; lies after downgrade — FOLDED: D11 write at every run startup + boot stamp + B14 downgrade direction.
- CT-7 (major) Staging crash ordering unspecified; ageout races pending; `clear_intent_keys` wipes pending — FOLDED: stage-PUT-first, separate `pending.json` (+preservation test), NotFound re-stage arm, ageout refresh (D7); product 2 actions. (Overlaps GD-4, OF-2.)
- CT-8 (major) preStop: no ack settlement, no fenced branch, unsized vs grace — FOLDED: D10 rules 1–3 (settle acks, refused-fenced branch, bounded retry, grace derivation), B11 variants. (Overlaps SD-7, GD-6, OF-4.)
- CT-9 (minor) Torn pending body from non-atomic agent write undefined — FOLDED: honored-as-bare-touch rule + agent temp-rename contract + injection test (D2.2).
- CT-10 (minor) Sync re-honor acks phantom conflicts — FOLDED: crc64 identity check before `sync-dirty` (§2.2), unit test.

**gc-durability**
- GD-1 (critical) Copy-then-CAS reintroduces the torn view; pod replacement installs a half-boundary permanently — FOLDED: citation-intent document + successor roll-forward (D7), product 2 mutation, leg B15; transient live window stated as residual §3.11 with Q2 as structural closure. (Same defect as MD-1.)
- GD-2 (major) On versioned buckets, Expiration reclaims nothing; lifecycle PUT is full-replace — FOLDED: NoncurrentVersionExpiration + ExpiredObjectDeleteMarker rules, version-scoped sweep deletes, DeleteObjectVersion probe, read-merge-append discipline (D8), B21 variant.
- GD-3 (major) Steady-state orphan minting; staging scales as churn × ageout — FOLDED: superseded-key deletes in the lane + post-CAS stage deletes; lifecycle demoted to crash backstop; §7 re-priced; B21 drain assertion.
- GD-4 (major) Age floor never pinned; NotFound arm unspecified; sweep age unnamed — FOLDED: CRD cross-validation K=2 (D8), NotFound re-stage arm (D7), sweep uses `eagerOrphanAgeoutDays` never 3600 (D8).
- GD-5 (major) Quiescence can never fire once anything is pending (diff-vs-baseline definition) — FOLDED: quiescence = scan-to-scan stability (§2.4.1.ii), leg B20 (FAIL on forced-lag-cap).
- GD-6 (major) D10 breaks the drain arithmetic; no retry; missing SIGTERM-ignoring control — FOLDED into D10 (per-object term, early-drain hook, bounded retry) + B11 variant (c). (Overlaps CT-8, SD-7, OF-4.)
- GD-7 (major) DR import semantics unstated; orphan evidence dies with the pod; ageout runs through the disaster — FOLDED: verbatim DR statement (§7b, CRD), durable `orphans.json`, staging-without-lease operator condition, DR guidance → Q1 (D9).
- GD-8 (minor) AdoptOwn vs 8-entry recent_uuids cap; lane window lifecycle unstated — FOLDED: recognizer pinned to `pending[path].flush_uuid` + crc64 (D7), product 2 mutation; window half resolved by CT-3's no-window rule.
- GD-9 (minor) "No manifest cites eager/" invariant load-bearing but unpinned — FOLDED: D7.1 dual invariant, enforced in `cas_write`, product 2 cite-in-place mutation; recovery tool bound to copy-then-cite.

**security-dos**
- SD-1 (critical) 1 s poll arm starves lease renewal and the floor barrier (verified against the shipped resettable-sleep select) — FOLDED: interval-based loop rewrite (D3) + D12; legs B16/B17.
- SD-2 (critical) Small-file sentinel spam = 12× PUT multiplier; constraint (b) false in request units; nonce-coalescing feeds re-touch loops — FOLDED: `sentinelHourlyBudget` defer-to-floor mechanism + covered-nonce-set acks + §7 rebuilt in requests. The suggested "skip re-PUT within cooldown on a forced barrier" is REJECTED as stated: excluding a dirty file's latest bytes from a sentinel barrier would falsify the ack's at-least guarantee (D1 corollary) — cost is bounded by deferring the barrier, never thinning it.
- SD-3 (major) Ack is a forgeable local file — FOLDED: ack documented advisory-only, authoritative remote signal named (§1.2, residual §3.6). MAC variant REJECTED: no intra-pod trust boundary exists to anchor it — a process that can forge the ack can already tamper with the workspace bytes the ack describes.
- SD-4 (major) Sync sentinel weaponizes `write_file_atomic` symlink traversal — FOLDED: containment-safe writes + scope normalization/component-boundary matching + bounds (§2.2), Phase 2 gate red-first.
- SD-5 (major) Gated lane window blocks HITL — FOLDED with CT-3 (§2.4.1, B19).
- SD-6 (major) Gateway boundary verb as fake InboxEntry misfires in `consume_inbox` (verified NotFound-arm conflict spam); polling contradiction — FOLDED: dedicated `boundary_request` field on the inbox doc, explicit consume logic, rides existing per-barrier GET, min-interval+budget applied (§2.5).
- SD-7 (major) preStop cannot finish inside grace — FOLDED into D10/backlog-cap/§2.6 grace derivation. The suggested "cite staged keys in place under time pressure" is REJECTED: it violates the D7.1/GD-9 invariant — a manifest citing `eager/` keys turns the lifecycle rule into a delayed destroyer of cited data.
- SD-8 (minor) Sentinel file-type confusion (FIFO/dir/symlink) hangs consume — FOLDED: S_ISREG lstat check, bounded 64 KiB read, note bounded 4 KiB, skip-and-warn (D2.1).

**ops-fleet**
- OF-1 (critical) `perFilePublishCooldownSecs=300` default regresses >64 MiB-file RPO under defaults — FOLDED: default 0 (= today), 300 documented opt-in, knob table gains a "default == shipped?" column, trade stated in §7b (§2.6).
- OF-2 (major) Gated lane diff base unspecified — re-stage storm or lifecycle eating a referenced stage — FOLDED: baseline ∪ pending diff base + no-re-stage test + ageout refresh + NotFound recovery + `staged_age_max_secs` gauge (§2.4.1, D7). (Overlaps CT-7, GD-4.)
- OF-3 (major) Knob changes never reach running pods; operator↔sidecar drift runs the wrong mode green; chart/CRD story wrong-shaped — FOLDED: heartbeat echo + `BoundaryModeActive` condition, propagation semantics stated, escape-hatch orphan hazard named (why `recover-staged` is v1), CRD-in-operator-binary + rollback schema-reversion documented (§2.6).
- OF-4 (major) preStop unsized on pure-spot fleets; replaced-pod RPO is the routine path — FOLDED: webhook grace derivation, backlog-cap source, B11 lag-bound-scale measurement, spot consequence verbatim in the CRD, recovery tool promoted to v1 (D9, D10). (Overlaps CT-8, SD-7, GD-6.)
- OF-5 (major) forced-lag-cap invisible downstream — coherence contract voided in the bucket — FOLDED: `flint-boundary-source` stamped in cas_write meta, forced-citation counter in gauges//status, B10 bucket-readable assertion, consumer guidance (§2.4.1.iii).
- OF-6 (major) No operator answer to "why is the manifest not advancing" before Phase 6 — FOLDED: Phase-3 observability minimum (withheld_reason line + gauge, heartbeat lag echo → conditions/printer columns, `flint-sync status`, probe cadence + condition) (§2.6, §5).
- OF-7 (major) §7 wrong at baseline (4-request tick, PUT-priced renew), never fleet-multiplied; B8 oracle would fail correct code — FOLDED: §7 rebuilt (real tick, D12 delta priced, fleet columns, proxy QPS, per-project storm exposure), B8 restated as delta-vs-recorded-control with the control shape in the leg.
- OF-8 (minor) remote.seq claims contradict (change-only writes vs updated_unix staleness tell) — FOLDED: heartbeat `updated_unix` every HEAD tick, change-only seq fields, agent staleness rule (D5), B7 assertion.
- OF-9 (minor) Pre-existing `.flint/` data: publish silently stops; sentinel-named files get eaten — FOLDED: D0.4 pre-flight disable + `SentinelVerbsActive` condition + `sentinels: auto|off|force` knob + breaking-change note + leg B22.

**model-drill**
- MD-1 (critical) Copy-then-CAS window unmodeled and undrilled; checkout abstraction no longer safe at boundary payloads — FOLDED with GD-1: Reader action in the substrate (§4), roll-forward, leg B15, residual §3.11, Q2 reopened.
- MD-2 (critical) "No new substrate" models the rejected in-place design; products 2/3 unfaithful or vacuous — FOLDED: claim retracted; real substrate (staged map, Copy, pending-with-base_etag, intent ghost, Reader) + abstraction-ledger obligations (§4).
- MD-3 (major) Missing products: gated×sync amputation, repair-only tombstones, boundary×takeover, D4×merge — FOLDED: D7 citation-time base-etag re-validation, repair-only withholds tombstones (§2.4.2), product 1 AllowStall variant, product 4 modeled before D4 code (§4, Phase 2 gate).
- MD-4 (major) No TLC affordability argument against the measured budget wall — FOLDED: per-cfg budget table, `MaxLanePasses` constant, 30-minute pilot obligation, stated-abstraction-only fallback (§4).
- MD-5 (major) Products 2/3 have zero probes — vacuous holds possible — FOLDED: required-violated probes `ProbeCitationInstalled`, `ProbeWithheldDelete`, `ProbeStragglerStagePut`, `ProbeLagCapForced`, `ProbeGC`@gated, plus product 4 probes (§4).
- MD-6 (major) Drill vacuity: B9 passes on a wedged probe, B4 needs no fresh ack, citation-crash and hybrid-regression legs missing, B14 image unpinned — FOLDED: B9 completed-checkout oracle, B4 fresh-ack requirement, new legs B15/B16, B14 pinned pre-D0 tag (§6).
- MD-7 (minor) Observed-red never stated; Phase 0 oracle has no liveness — FOLDED: red-first discipline sentence + named honest red form for D0 + Phase 0 control-liveness requirement (§5).

---

**Decision index:** D0 `.flint/` reservation + delete exemption + pre-existing-data pre-flight · D1 at-least boundary semantics (contents never thinned) · D2 rename-based exactly-once + settle-before-consume + refused-fenced acks + uniform restart rule · D3 interval poll / coalesce / min-interval / hourly budget / covered-nonce acks · D4 scoped-sync inst_base rule + scope validation (modeled before code) · D5 remote.seq off the existing HEAD + heartbeat updated_unix · D6 three modes, hybrid default; windowless upload lane; four citation sources; bucket-visible source stamp · D7 staging namespace, pending.json, stage-first ordering, base-etag re-validation, pinned AdoptOwn, NotFound arm, citation-intent roll-forward, copy-cite only (manifest keys under `files/` — enforced), HITL exemption with tombstone withholding · D8 prefix-scoped lifecycle ageout, versioned rules, read-merge-append, K=2 cross-validation, sweep age = ageout · D9 takeover surfacing durable + `recover-staged` in v1, auto-adopt never (roll-forward excepted) · D10 preStop cites everything, settles acks, bounded retry, sized against grace, backlog-capped · D11 capability marker at every startup + boot stamp · D12 lease-renewal discipline (renew-before-barrier + 30 s heartbeat on non-resettable timers). Open: §8 Q1–Q6.