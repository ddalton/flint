[package]
name = "spdk-benchmark"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
serde_json = "1.0"

[build-dependencies]
bindgen = "0.69"
pkg-config = "0.3"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
