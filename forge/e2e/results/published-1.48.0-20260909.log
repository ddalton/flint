# v1.48.0 — THE PUBLISHED ARTIFACT, 2026-09-09
#
# The chart installed as shipped, images pulled from Docker Hub by
# digest, nothing built from the checkout and no tag overridden. Run on
# a throwaway kind cluster (created and deleted for this run; the zero
# set was verified with a positive control that kind can still SEE a
# cluster it just made).
#
# ── forge: 28/0/0 ───────────────────────────────────────────────────
# The three images the drill resolved:
#   flint-forge-git      @sha256:ea3aeabded3cecfed91fae190ad75cfd2875d685cf299086362546ad9b8bdda9
#   flint-forge-operator @sha256:27a3f7bb7452283c0054cd2c05591c48bce9117ab2f6e4e1a7bbf03e356b8a90
#   flint-forge-syncer   @sha256:1576fc76e2a462cee25c5f11bf7ded44845fc0fc2e3783ba9671ae41a1d81358
#
# ── lite: the half with no drill of its own ──────────────────────────
# flint-lite 0.4.14 PULLED FROM THE REGISTRY (not the checkout),
# rendered, installed, hub Running. The pod's imageID resolved to
# docker.io/dilipdalton/flint-pnfs@sha256:4fdcda4168c2e0a679d8f972dfbc74e214a875036537ccec372a985f3d5d09ed
# — the same digest `publish-images.sh` printed for the manifest list,
# so the published chart pulls the published image.
#
# THE RUNNING BINARY CARRIES THE FIX THIS RELEASE IS NAMED FOR, checked
# by content rather than by tag: `grep -c "the bucket does not hold this
# file" /usr/local/bin/flint-pnfs-mds` inside the pod returned 6, and
# the negative control (a string that cannot be there) returned nothing,
# so the 6 is a measurement and not a grep that always matches.
#
# AND THE BUILD-PROFILE LINE IS GONE. Before this release the hub
# printed "MDS SERVER BINARY VERSION: DEBUG BUILD" unconditionally, so a
# --release binary announced itself as a debug build in the one line an
# operator reads to learn what is running. 149 log lines from the
# released pod, zero matches for "BINARY VERSION" — with the line count
# as the control that the log was readable at all.
#
# ── the full forge transcript ────────────────────────────────────────
published-artifact drill — context kind-forge-pub, chart /Users/ddalton/github/flint-1480-clean/flint-forge-chart
  ..  chart names: dilipdalton/flint-forge-git:1.48.0
  ..               dilipdalton/flint-forge-syncer:1.48.0
  ..               dilipdalton/flint-forge-operator:1.48.0

── P0 — every image the chart names is actually published
  ok: dilipdalton/flint-forge-operator:1.48.0 is on the registry
  ok: dilipdalton/flint-forge-git:1.48.0 is on the registry
  ok: dilipdalton/flint-forge-syncer:1.48.0 is on the registry

── P1 — the chart names the newest published tag, not an older one
  ok: the chart is current at 1.48.0

── P2 — the images are PULLED from the registry, not found in the node cache
  ..  cleared 0 cached image(s) from 1 node(s)
  ok: the node cache was cleared before install (the pull below cannot be satisfied locally)

── P3 — helm install the chart as shipped — no --set of any image
  ok: the store is up and the bucket exists
  ok: the chart installed with its own image defaults

── P4 — a FlintRepo becomes a serving repository
  ok: the chart installed the FlintRepo CRD
  ok: the FlintRepo was accepted by the apiserver
  ok: agents/proj reached Ready in 10s
  ok: the agent pod is running the published forge-git image
  ok: all 3 distinct flint image(s) carry a registry digest — operator, git and syncer all came from Docker Hub
        · docker.io/dilipdalton/flint-forge-git@sha256:ea3aeabded3cecfed91fae190ad75cfd2875d685cf299086362546ad9b8bdda9
        · docker.io/dilipdalton/flint-forge-operator@sha256:27a3f7bb7452283c0054cd2c05591c48bce9117ab2f6e4e1a7bbf03e356b8a90
        · docker.io/dilipdalton/flint-forge-syncer@sha256:1576fc76e2a462cee25c5f11bf7ded44845fc0fc2e3783ba9671ae41a1d81358

── P5 — clone, commit and push — and the BUCKET names the ref before the client is told ok
  ok: the writer seeds main via refs/for/main
  ok: an agent branch push is accepted
  ok: the bucket's snapshot names agent/pub-1788990341
  ..  agent branch tip c091a4fb05808f8051c4a243466784ee49c71bdb

── P6 — main is protected, and the refusal names the remedy
  ok: a direct push to protected main (refused)
        ·  ! [remote rejected] main -> main (refs/heads/main is protected: push to refs/for/main to propose a merge)
  ok: the same change proposed through refs/for/main merges
  ok: main now carries it

── P7 — the published forge-git image carries the 'git propose' verb
  ok: an absent verb reports 'is not a git command' — the dispatch check has teeth
  ok: git propose proposes into the default branch

── P8 — the pod is destroyed and the published image restores from the bucket alone
  ..  server pod deleted — the emptyDir went with it, so the bucket is all that is left
  ok: main is at the same commit after the restore (dac95b212f9f4f89b3b170674f8d99f40bc890c3)
  ok: a FRESH clone from the restored repository passes git fsck --strict

── P9 — the chart's default leaves /repo/<ns>/<name>/files unrouted
  ..  default install: git route -> 401, files route -> 404
  ok: the git door on the same listener answers (401) — the port and the path are right
  ok: the file route is absent by default (404, no route mounted)
  ok: the rendered door args do not carry --repo-files

── P10 — setting door.repoFiles.enabled opens exactly that route, on the same image and port
  ok: the rendered door now carries --repo-files
  ..  with the door open: git route -> 401, files route -> 401
  ok: the same URL now reaches the handler (401) — so P9's 404 was the flag, not a dead route
  ok: the git route is unchanged at 401 — one listener, two tables

══ published artifact: 28 passed, 0 failed, 0 pending ══
   GREEN
KEEP=1: forge-system and agents left standing
