# React + TypeScript + Vite

build docker image using command:
docker build -t spdk-dashboard . -f Dockerfile.dashboard

Run docker image using command:
docker run -p 8080:80 spdk-dashboard

Access app using URL:
http://localhost:8080/


