\* THE DEFAULT PATH, UNREPAIRED — a tgt dies with nobody's permission and the
\* volume never comes back.
\*
\* THE DESTROYER IS NOT THE ROLLER.  UncontrolledTgtDeath arms TgtDie(l): a
\* csi-node tgt dies with node and consumer in place, gated on no maintenance
\* state whatsoever — no `rolling`, no marks, no lease, no barrier, no
\* refusal.  Class-3 destruction previously existed ONLY inside RollStart, so
\* every F62/F63 run asked "can flint's roller cause this?" about a feature
\* that is OFF BY DEFAULT and which, in that default configuration, refuses to
\* act at all (plan_roll returns Blocked when !on_delete, maint_roll.rs:248).
\*
\* The operative question is whether a routine `helm upgrade` can, and the
\* chart answers it: updateStrategy: OnDelete is emitted only inside the
\* maintenance.drainRoll.enabled conditional (node.yaml:13-24), and that flag
\* defaults FALSE — so the shipped DaemonSet takes k8s's RollingUpdate and
\* rolls every node pod on a template change.  OOM kills, kubelet restarts,
\* evictions, node-image upgrades and GitOps syncs land here too.
\*
\* MaintEnabled is still TRUE only so the roll theorems stay armed; nothing in
\* these runs needs the roller, and TgtDie ignores it.
\*
\* Neither repair is armed: DpSeenRehydrate = FALSE (A1's detector is blind,
\* its HashSet died with the process) and RaidReconcileArm = FALSE (no A2).
\* The whole bounce chain is armed and willing, and it does not matter,
\* because nothing can report a collapse that the reporting process cannot
\* remember.
\*
\* EXPECTED: VIOLATED.  This is a PROBE, not a theorem, and the paragraphs
\* above describe FlintReplicationUncontrolledBlind.cfg — they were copied
\* from it and the verdict line came with them.  Corrected 2026-07-31.
\*
\* ProbeTgtDeathReachable == ~raidLostOnce, and raidLostOnce has exactly one
\* writer, so a VIOLATION is the witness that the class-3 tgt death really
\* occurs in this configuration.  If this run ever went green it would mean
\* the death never happens — which would make the Blind run's "no
\* interleaving recovers the volume" a statement about a world where nothing
\* was ever lost, i.e. vacuous.  That is the whole reason the probe exists.
\*
\* It did not run at all until 2026-07-31.  check-tla.sh cited it by name as
\* the Blind green's non-vacuity licence, in the run label and again in the
\* catalogue, while no invocation executed it — a green licensed by a run the
\* gate does not perform.  FlintA2Probe.tla:24-25 states the rule this broke.
SPECIFICATION SpecRaidRepair
CONSTANTS
  Legs = {l1, l2}
  MaxWrites = 1
  MaxCrashes = 0
  GateStrict = TRUE
  RejoinGuard = TRUE
  FenceZombie = TRUE
  ClaimArb = TRUE
  EvidenceStrict = TRUE
  MaintEnabled = TRUE
  LocalLegs = {l2}
  MaintProcessedGate = TRUE
  RefusalSticky = FALSE
  ConsumerMobile = FALSE
  RaidLifetimeArm = TRUE
  MaintLocalRefuse = TRUE
  RaidReconcileArm = FALSE
  VaCanLag = FALSE
  NodeStageValidatesBases = FALSE
  StrikeRepairArm = FALSE
  A2LocalStagingBelt = FALSE
  A2SoleOwnershipBelt = FALSE
  UncontrolledTgtDeath = TRUE
  DpSeenRehydrate = FALSE
  MaintFence = TRUE
  MaintBarrier = TRUE
  MaintLease = TRUE
  BarrierRaidAware = TRUE
  ExpandEnabled = FALSE
  SizeGuard = TRUE
  SizeHeal = TRUE
  GateDeadline = FALSE
  StaleFloor = FALSE
  MonitorCurrent = TRUE
  DeviceFloor = TRUE
  SuppressScoped = FALSE
  DrainBelt = TRUE
  RollerRace = FALSE
  RollerLeaderGate = TRUE
  DrainMarksBelt = TRUE
  BounceEnabled = TRUE
  MaxBounces = 1
  AdmissionArm = FALSE
  DataPathArm = TRUE
  BouncePreflight = TRUE
  BounceRace = FALSE
  BounceLeaderGate = TRUE
  StageAdmit = FALSE
  PodLayer = FALSE
  ReconcilerBelt = TRUE
  DetachWaitHonored = TRUE
  PlannerDisjoint = FALSE
  WriterLimbo = FALSE
  RefusalBounded = FALSE
  KubeDsArm = FALSE
  ReadyScope = "socket"
  PodTrigger = "none"
  MinReadySecsArm = FALSE
  MaxKubeEvents = 0
  GraceExceedsRecovery = FALSE
INVARIANTS
  ProbeTgtDeathReachable
CHECK_DEADLOCK FALSE
CONSTRAINT
  GenBound
