\* A2 ADOPT, UNGUARDED — NodeStage inherits the phantom whole.
\*
\* THE ADOPT. `ensure_raid1_bdev` (driver.rs:3105) is create-OR-CONVERGE, and
\* its converge branch REUSES any raid of this name in state "online" without
\* comparing the base set to the one NodeStage intended ("already ONLINE (N
\* base(s) configured) — reusing"; that count reaches the log line and nowhere
\* else). Ordinary Assemble cannot represent it — Assemble requires
\* serving = {}, and an online raid has at least one configured base — so
\* AssembleAdopt is its own action.
\*
\* NOT a bug report against shipped code: with NodeStage the sole creator, an
\* online raid of that name is one NodeStage itself built from the same PV
\* replica record, so reuse is a correct idempotent restage. It becomes a
\* hazard only once A2 is a second creator whose base set was chosen at a
\* different time.
\*
\* Naive A2 (VA only) + the shipped unconditional reuse. The consumer leaves a
\* node, A2 rebuilds there on the stale VA, and when the consumer RETURNS
\* NodeStage short-circuits onto A2's object: serving, writerSet, lineage and
\* legGen all inherited, chosen by a boot-time snapshot taken while the volume
\* was somebody else's. The F44/F46 adopt-or-mint family by a new road.
\*
\* The ghost is stamped only by AssembleAdopt and only on A2 provenance, so the
\* violation is attributable to this door alone — the pod-layer tranche's rule
\* that a mutation run whose invariant is violable by other means proves
\* nothing about the mutation.
\*
\* EXPECTED: Inv_NoAdoptOfA2Composition VIOLATED.
\* scripts/check-tla.sh runs this and requires the violation.
SPECIFICATION SpecConsumerMobile
CONSTANTS
  Legs = {l1, l2}
  MaxWrites = 1
  MaxCrashes = 0
  GateStrict = TRUE
  RejoinGuard = TRUE
  FenceZombie = TRUE
  ClaimArb = TRUE
  EvidenceStrict = TRUE
  MaintEnabled = TRUE
  LocalLegs = {l2}
  MaintProcessedGate = TRUE
  RefusalSticky = FALSE
  ConsumerMobile = TRUE
  RaidLifetimeArm = TRUE
  MaintLocalRefuse = TRUE
  RaidReconcileArm = TRUE
  VaCanLag = TRUE
  NodeStageValidatesBases = FALSE
  A2LocalStagingBelt = FALSE
  A2SoleOwnershipBelt = FALSE
  UncontrolledTgtDeath = FALSE
  DpSeenRehydrate = FALSE
  MaintFence = TRUE
  MaintBarrier = TRUE
  MaintLease = TRUE
  BarrierRaidAware = TRUE
  ExpandEnabled = FALSE
  SizeGuard = TRUE
  SizeHeal = TRUE
  GateDeadline = FALSE
  StaleFloor = FALSE
  MonitorCurrent = TRUE
  DeviceFloor = TRUE
  SuppressScoped = FALSE
  DrainBelt = TRUE
  RollerRace = FALSE
  RollerLeaderGate = TRUE
  DrainMarksBelt = TRUE
  BounceEnabled = FALSE
  MaxBounces = 0
  AdmissionArm = FALSE
  DataPathArm = FALSE
  BouncePreflight = TRUE
  BounceRace = FALSE
  BounceLeaderGate = TRUE
  StageAdmit = FALSE
  PodLayer = FALSE
  ReconcilerBelt = TRUE
  DetachWaitHonored = TRUE
  PlannerDisjoint = FALSE
  WriterLimbo = FALSE
  RefusalBounded = FALSE
INVARIANTS
  Inv_NoAdoptOfA2Composition
CHECK_DEADLOCK FALSE
CONSTRAINT
  GenBound
