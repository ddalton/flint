\* THE ROLL THEOREMS WITH THE A2 ARM SET — and NOT a test of A2.
\*
\* ⚠️ RELABELLED 2026-07-30. This file used to be headed "the belt that works,
\* and the deliverable of this tranche", and that was false. Over the COMPLETE
\* state graph of this configuration — 7,375,538 generated, 1,261,953 distinct,
\* 0 left on queue — `AgentBootReconcile` fires ZERO times. Verified directly:
\* ProbeA2Fires == a2Created = {} HOLDS here, and a2Created has exactly one
\* writer. Every claim this run was cited for (the local-staging belt,
\* Inv_SingleComposition, Inv_A2AssemblesOnlyAtTruth) rested on a run in which
\* the belted action was disabled from beginning to end.
\*
\* WHY, precisely: A2 answers a CLASS-3 destroyer — a tgt dying with node and
\* consumer in place, which leaves kubelet's `staged` belief SET and therefore
\* has no inverse. The belt requires exactly that (`stagedAt = vaNode`). This
\* cfg pins UncontrolledTgtDeath = FALSE, so the only reachable destroyers are
\* the ones that CLEAR staging (pod delete, relocation). Belt precondition and
\* available destroyers were mutually exclusive: the guard was unsatisfiable by
\* construction, not merely unexercised.
\*
\* The header below this line USED to make the argument that made it plausible,
\* and it is worth keeping the shape of the mistake visible: the reasoning about
\* the belt is still correct, it simply was never tested here.
\*
\* THE BELT CLAIMS NOW LIVE IN FlintReplicationA2Armed.cfg, which flips that one
\* constant, drops the roll theorems as vacuous (no roller there), and is paired
\* with FlintA2ProbeArmed.cfg — a gate run that REQUIRES a violation, so this
\* can never silently recur.
\*
\* WHAT THIS RUN STILL HONESTLY PROVES: the four roll theorems and the base
\* invariant hold under a roller with the A2 constant set. That is a real
\* statement about the ROLLER. It is not a statement about A2.
\*
\* The belt's design argument, retained because it is sound and is what
\* A2Armed now actually tests: A2 assembles only where kubelet ALSO still
\* believes the volume staged. A class-3 destroyer is DEFINED by leaving
\* `staged` alone and a relocation is DEFINED by clearing it, so this admits
\* exactly the case A2 exists for and refuses exactly the case that
\* manufactures a phantom — no cluster-wide probe, no lease, no superblock,
\* nothing remembered across the restart. And A2 leaves staged/stagedAt
\* untouched, because a node agent re-creating an SPDK object does not
\* participate in kubelet's bookkeeping; an earlier draft had it write
\* stagedAt' = vaNode, which would let A2 satisfy this very belt with its own
\* previous action. The belt is only worth anything because its input is
\* somebody else's.
\*
\* EXPECTED: green.  scripts/check-tla.sh runs this and requires success.
SPECIFICATION SpecConsumerMobile
CONSTANTS
  Legs = {l1, l2}
  MaxWrites = 1
  MaxCrashes = 0
  GateStrict = TRUE
  RejoinGuard = TRUE
  FenceZombie = TRUE
  ClaimArb = TRUE
  EvidenceStrict = TRUE
  MaintEnabled = TRUE
  LocalLegs = {l2}
  MaintProcessedGate = TRUE
  RefusalSticky = FALSE
  ConsumerMobile = TRUE
  RaidLifetimeArm = TRUE
  MaintLocalRefuse = TRUE
  RaidReconcileArm = TRUE
  VaCanLag = TRUE
  NodeStageValidatesBases = FALSE
  StrikeRepairArm = FALSE
  A2LocalStagingBelt = TRUE
  A2SoleOwnershipBelt = FALSE
  UncontrolledTgtDeath = FALSE
  DpSeenRehydrate = FALSE
  MaintFence = TRUE
  MaintBarrier = TRUE
  MaintLease = TRUE
  BarrierRaidAware = TRUE
  ExpandEnabled = FALSE
  SizeGuard = TRUE
  SizeHeal = TRUE
  GateDeadline = FALSE
  StaleFloor = FALSE
  MonitorCurrent = TRUE
  DeviceFloor = TRUE
  SuppressScoped = FALSE
  DrainBelt = TRUE
  RollerRace = FALSE
  RollerLeaderGate = TRUE
  DrainMarksBelt = TRUE
  BounceEnabled = FALSE
  MaxBounces = 0
  AdmissionArm = FALSE
  DataPathArm = FALSE
  BouncePreflight = TRUE
  BounceRace = FALSE
  BounceLeaderGate = TRUE
  StageAdmit = FALSE
  PodLayer = FALSE
  ReconcilerBelt = TRUE
  DetachWaitHonored = TRUE
  PlannerDisjoint = FALSE
  WriterLimbo = FALSE
  RefusalBounded = FALSE
INVARIANTS
  Inv
  Inv_SingleComposition
  Inv_A2AssemblesOnlyAtTruth
  Inv_StagedAgrees
  Inv_RaidCompositionCoupled
  Inv_PlannedRollNeverCausesOutage
  Inv_MaintFenceHolds
  Inv_MaintFenceStrict
  Inv_PlannedRollBoundedImpact
PROPERTIES
  RelocationWindowCloses
  MaintenanceEventuallyLifts
CHECK_DEADLOCK FALSE
CONSTRAINT
  GenBound
