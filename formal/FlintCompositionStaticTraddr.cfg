\* THE FORWARD LIVELOCK AS A LASSO — the review's sequencing finding
\* ("forward: while the config names the dead node, every preempt fails,
\* delivered stays 0, and revoke is refused UnconfirmedFence — forever"),
\* now with teeth.  PreemptFollowsRecord = FALSE pins the fence preempt
\* to the ORIGINAL composer's address — the shipped code's shape, where
\* traddr is reconciler constructor config (block_export.rs:55-59,
\* 517-524), not per-volume state.  TLC must FIND the temporal
\* counterexample under FULL fairness: the failover completes, the
\* survivor serves, the fenced client even re-attaches — and every
\* preempt dials the dead node until the end of time, so the
\* confirmation never lands and every parked range the quarantine sweep
\* guards stays parked.  The fix this run demands is the target
\* registry: preempt targets resolve from the record, per pass.
SPECIFICATION SpecLive
CONSTANTS
  Tgts = {tA, tB}
  Clients = {c1, c2}
  MaxEpoch = 2
  MaxCrashes = 2
  LegAdmissionGate = TRUE
  DeadmanGate = TRUE
  DeadmanCertain = TRUE
  RecordAssemblyOnly = TRUE
  ElectInSync = TRUE
  DegradeBarrier = TRUE
  FreeWaitsActive = TRUE
  FenceReplayOnAssemble = TRUE
  DeliveredEpochKeyed = FALSE
  RejoinEnabled = TRUE
  RecordRejoinOnly = TRUE
  UncleanResync = TRUE
  AncestryGuard = TRUE
  PreemptFollowsRecord = FALSE
PROPERTIES
  FenceEventuallyConfirmed
CHECK_DEADLOCK FALSE
