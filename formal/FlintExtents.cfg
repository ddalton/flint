\* THE SHIPPED-DESIGN WORLD.  Every belt the design commits to is ON;
\* FenceReaches is FALSE because spdk-tgt reservation enforcement has not
\* been proven on real hardware, and a constant encoding an assumption
\* silently becomes a lie.  Consequently the two theorems
\* (Inv_NoStaleExtentRead/Write) are deliberately NOT listed — they are
\* reachable-false in this world, exactly as Inv_NoStaleServe is absent
\* from FlintTruncate.cfg, and FlintExtentsLostFence.cfg is the run that
\* proves it.  What IS claimed: no conflicting grants, and no reuse under
\* a live unfenced grant.  Citable only alongside the four Probe runs.
SPECIFICATION Spec
CONSTANTS
  Clients = {c1, c2}
  NBlocks = 3
  MaxGrants = 4
  MaxReclaims = 2
  MaxTgtRestarts = 1
  GrantsExclusive = TRUE
  RecallBeforeReuse = TRUE
  FreeRevalidates = TRUE
  FenceReaches = FALSE
  PersistReservations = TRUE
  CommitEnabled = FALSE
  ProvisionalInvisible = TRUE
  CommitGatesSize = TRUE
  CommitChecksGen = TRUE
INVARIANTS
  Inv
CHECK_DEADLOCK FALSE
