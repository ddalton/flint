\* THE TARGET STATE — conditional, not achieved.
\*
\* Everything the fix needs, all on: the recall fires, it is DELIVERED AND
\* HONOURED (RecallReaches), and a layout published across the mark boundary
\* re-reads the gate and is revoked (PublishRecheck). Inv_NoStaleServe HOLDS.
\*
\* This run says what closing F65 requires. It does NOT say F65 is closed —
\* the shipped code satisfies NEITHER premise today (audit C1/C2/C3 for
\* delivery, C6 for the publish race). Read it as the specification of the
\* remaining work, and read FlintTruncate.cfg for where the code actually is.
SPECIFICATION Spec
CONSTANTS
  DSes = {d1, d2}
  Clients = {c1}
  MaxOff = 2
  MaxSets = 3
  MaxJobs = 2
  GateClearGuarded = TRUE
  MarkKeepsMin = TRUE
  RecallOnTruncate = TRUE
  RecallReaches = TRUE
  PublishRecheck = TRUE
INVARIANTS
  Inv
  Inv_NoStaleServe
CHECK_DEADLOCK FALSE
