\* RESIDUAL PROBE (required to fail against the UNMUTATED protocol):
\* Inv_NoStaleLand.  A publish planned under a valid reign lands after
\* the depose+takeover interleaves between plan and land — plan and
\* land are separate store steps and S3 has no cross-object CAS to bind
\* the session cell to the data key.  FlintTierEpochProbeStale's window
\* at the session layer.  BOUNDED by ZombieOwnershipResolves (strict
\* liveness: the fence closes it) and arbitrated by the data plane's
\* epoch stamps (FlintTierEpoch's Inv_NoSuccessorOverwrite theorem) —
\* the residual costs the successor one failed flush cycle, never
\* correctness.
SPECIFICATION Spec
CONSTANTS
  Hubs = {h1, h2}
  Vols = {v1, v2}
  Misses = 2
  MaxBeats = 2
  MaxPubs = 2
  MaxCrashes = 1
  MaxClaims = 3
  Depose = TRUE
  SessRotate = TRUE
  FenceOnFail = TRUE
  DrainOnRelease = TRUE
INVARIANTS
  Inv_NoStaleLand
CHECK_DEADLOCK FALSE
