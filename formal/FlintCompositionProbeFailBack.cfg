\* TRANCHE-2 NON-VACUITY PROBE: strict rejoin-world constants, and TLC
\* must VIOLATE ProbeFailBackCompletes — a witness of the record
\* machine's full round trip: partition the composer, promote the
\* survivor, rebuild the old leg record-driven, lose the survivor, and
\* promote BACK to a leg whose in-sync mark was EARNED by a completed
\* rebuild.  This is the trace that makes the sticky-stale design honest:
\* stale marks clear through exactly one door, and the election gate can
\* trust what it reads.
SPECIFICATION Spec
CONSTANTS
  Tgts = {tA, tB}
  Clients = {c1, c2}
  MaxEpoch = 3
  MaxCrashes = 2
  LegAdmissionGate = TRUE
  DeadmanGate = TRUE
  DeadmanCertain = TRUE
  RecordAssemblyOnly = TRUE
  ElectInSync = TRUE
  DegradeBarrier = TRUE
  FreeWaitsActive = TRUE
  FenceReplayOnAssemble = TRUE
  DeliveredEpochKeyed = FALSE
  RejoinEnabled = TRUE
  RecordRejoinOnly = TRUE
  UncleanResync = TRUE
  AncestryGuard = TRUE
INVARIANTS
  ProbeFailBackCompletes
CHECK_DEADLOCK FALSE
