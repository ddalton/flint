\* The F56 mutation — the SHIPPED pre-fix world: SizeHeal = FALSE (no
\* align_dst_head_size; the guard defers instead of growing).  TLC must
\* FIND the permanent livelock as an ExpansionCompletes lasso: a leg lost
\* mid-fan-out returns as a live, content-warm, size-old STANDBY; the
\* admission size guard defers it every tick, the C2 belt refuses the
\* expand retry that would grow it, resize_lvol has no other caller, and
\* the chase's retention pin keeps the base epoch alive so the
\* source-sized full build never takes over.  Four individually-correct
\* mechanisms, jointly starving — rediscovered as a temporal
\* counterexample, the same shape as F43.  2026-07-29 audit: this run
\* DOUBLES as the fixed code's residual — align_dst_head_size failure is
\* a session-aborting defer at both call sites with no counter and no
\* escalation, so under a DETERMINISTICALLY failing bdev_lvol_resize the
\* fixed code loops revert→align-fail→defer in exactly this lasso.
\* SizeHeal = TRUE in the strict run is therefore an explicit
\* heal-eventually-succeeds assumption, and the wave-2 code work owes an
\* event (each failed admission attempt also leaks one epoch — the align
\* runs after the final cut).  scripts/check-tla.sh requires the
\* violation.
SPECIFICATION SpecExpand
CONSTANTS
  Legs = {l1, l2}
  MaxWrites = 1
  MaxCrashes = 2
  GateStrict = TRUE
  RejoinGuard = TRUE
  FenceZombie = TRUE
  ClaimArb = TRUE
  EvidenceStrict = TRUE
  MaintEnabled = FALSE
  LocalLegs = {}
  MaintProcessedGate = TRUE
  RaidLifetimeArm = FALSE
  MaintLocalRefuse = FALSE
  RaidReconcileArm = FALSE
  DpSeenRehydrate = FALSE
  MaintFence = TRUE
  MaintBarrier = TRUE
  MaintLease = TRUE
  BarrierRaidAware = TRUE
  ExpandEnabled = TRUE
  SizeGuard = TRUE
  SizeHeal = FALSE
  GateDeadline = FALSE
  StaleFloor = FALSE
  MonitorCurrent = TRUE
  DeviceFloor = TRUE
  SuppressScoped = TRUE
  DrainBelt = TRUE
  RollerRace = FALSE
  RollerLeaderGate = TRUE
  DrainMarksBelt = TRUE
  BounceEnabled = FALSE
  MaxBounces = 0
  AdmissionArm = FALSE
  DataPathArm = FALSE
  BouncePreflight = TRUE
  BounceRace = FALSE
  BounceLeaderGate = TRUE
  StageAdmit = FALSE
  PodLayer = FALSE
  ReconcilerBelt = TRUE
  DetachWaitHonored = TRUE
  PlannerDisjoint = FALSE
  WriterLimbo = FALSE
  RefusalBounded = FALSE
INVARIANTS
  Inv
  Inv_NoDeviceShrink
PROPERTIES
  ExpansionCompletes
CHECK_DEADLOCK FALSE
CONSTRAINT
  GenBound
