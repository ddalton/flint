\* THE PEER-ARBITRATION REFUTATION AS A RUN — the reason the arbiter fork
\* was decided for the etcd witness, kept as a counterexample the way
\* StaleSnapshotFree keeps the refuted grant-side sketch.  Single flag off
\* the Witness strict: MarkAtWitness = FALSE, the degrade's stale mark
\* lands in the composer's OWN record — the write that is available
\* exactly when the witness is not, which is the whole temptation, and
\* the write any two-record design is forced into when the arbiter for
\* the mark is the unreachable peer itself.  The barrier opens on the
\* local mark, solo acks flow legitimately — and the ELECTION reads the
\* witness, which was never told: legSync still says insync, ElectInSync
\* admits the returned peer in good faith, and its assembly discards
\* every acked solo write.  TLC must FIND Inv_NoDoomedAck violated.  The
\* degraded-window counterexample of tranche 1 (DegradeBlind), arriving a
\* third way: not a dropped belt, not stock raid1's ack path — a belt
\* fully ON, reading a fact the elector cannot see.
SPECIFICATION Spec
CONSTANTS
  Tgts = {tA, tB}
  Clients = {c1, c2}
  MaxEpoch = 2
  MaxCrashes = 2
  LegAdmissionGate = TRUE
  DeadmanGate = TRUE
  DeadmanCertain = TRUE
  RecordAssemblyOnly = TRUE
  ElectInSync = TRUE
  DegradeBarrier = TRUE
  FreeWaitsActive = TRUE
  FenceReplayOnAssemble = TRUE
  DeliveredEpochKeyed = FALSE
  RejoinEnabled = TRUE
  RecordRejoinOnly = TRUE
  UncleanResync = TRUE
  AncestryGuard = TRUE
  PreemptFollowsRecord = TRUE
  MaxCuts = 2
  MarkAtWitness = FALSE
  FenceAtWitness = TRUE
INVARIANTS
  Inv_NoDoomedAck
CHECK_DEADLOCK FALSE
