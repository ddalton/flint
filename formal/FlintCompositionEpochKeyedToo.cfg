\* THE MACHINE-CHECKED REDUNDANCY VERDICT (the MergeMin precedent: state
\* the choice the code did NOT make, run it under the full theorem stack,
\* and let the green say it is safety-irrelevant).  DeliveredEpochKeyed =
\* TRUE adopts the review's delivered-keyed-by-epoch schema refinement on
\* top of every shipped belt.  GREEN here, together with the shipped
\* strict cfg's green at FALSE, is the pair that licenses NOT adopting
\* it: with the fence replay at the export mouth and the free waiting on
\* ActiveNew, keying the delivered bit by epoch adds guards but closes
\* nothing that is open and opens nothing that is closed.  The
\* fenced_clients schema therefore stays as FreeRequiresDelivered has it.
\* (What the keying was FOR is honestly closed elsewhere:
\* FlintCompositionNoReplay.cfg shows the hole and names the belt that
\* actually closes it.)
SPECIFICATION Spec
CONSTANTS
  Tgts = {tA, tB}
  Clients = {c1, c2}
  MaxEpoch = 2
  MaxCrashes = 2
  LegAdmissionGate = TRUE
  DeadmanGate = TRUE
  DeadmanCertain = TRUE
  RecordAssemblyOnly = TRUE
  ElectInSync = TRUE
  DegradeBarrier = TRUE
  FreeWaitsActive = TRUE
  FenceReplayOnAssemble = TRUE
  DeliveredEpochKeyed = TRUE
  RejoinEnabled = FALSE
  RecordRejoinOnly = TRUE
  UncleanResync = TRUE
  AncestryGuard = TRUE
  PreemptFollowsRecord = TRUE
  MaxCuts = 0
  MarkAtWitness = TRUE
  FenceAtWitness = TRUE
INVARIANTS
  TypeOK
  Inv_NoDivergentServing
  Inv_NoDoomedAck
  Inv_NoStaleExtentWrite
  Inv_NoStaleServe
CHECK_DEADLOCK FALSE
