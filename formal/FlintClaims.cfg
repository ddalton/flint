\* Strict run: the shipped stack — the P1 Lease (LeaderGate) and the F50
\* marker grace both ON, two processes, deaths and spurious leadership
\* moves in budget.  Every invariant and both liveness properties must
\* HOLD — including the full crash-recovery story (owner dies mid-window,
\* marker ages, the survivor takes the lease, scrubs, re-warms, re-opens,
\* commits).  scripts/check-tla.sh runs this and requires success.
SPECIFICATION Spec
CONSTANTS
  Procs = {p1, p2}
  MaxCrashes = 2
  LeaderGate = TRUE
  MarkerGrace = TRUE
INVARIANTS
  Inv
PROPERTIES
  WindowResolves
  EventuallyServes
CHECK_DEADLOCK FALSE
