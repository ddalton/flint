\* THE C5-DRIFT LANE (FenceComplete=FALSE): a mutation lane that never
\* consults the fence at all — the LINK class the design's verifier
\* caught missing from the site inventory.  The executable completeness
\* check (fence sites == production bump lanes) is what keeps this world
\* impossible in code.  TLC must FIND the unsignalled stale believer.
SPECIFICATION Spec
CONSTANTS
  MaxMut = 2
  MaxGrants = 2
  MaxRestarts = 1
  MaxDies = 1
  MutationGuard = TRUE
  FenceComplete = FALSE
  DisownEvidence = TRUE
  RebindRearm = TRUE
  PersistHolderEvidence = TRUE
  LadderRecheck = TRUE
INVARIANTS
  Inv_NoUnsignalledStaleness
CHECK_DEADLOCK FALSE
