\* THE SHIPPED WORLD'S REDIRECT HOLE AS A LASSO (the SpecNoP4 pattern):
\* SpecNoRedirect is SpecLive minus exactly one obligation — the redirect
\* actor's — which is the world the review found: reestablish_sessions
\* replays the recorded traddr with a deliberate "No MDS call",
\* AttachBlockNode answers static per-shard config, and the only
\* production CB_NOTIFY_DEVICEID sender is expand.  TLC must FIND the
\* temporal counterexample: failover completes, the survivor serves, and
\* a client sits parked at the dead node's address FOREVER while every
\* other obligation in the system is discharged.  This run is the
\* machine-checked statement of what building the re-attach lane +
\* session-up ack + per-client notify re-fire is FOR.
SPECIFICATION SpecNoRedirect
CONSTANTS
  Tgts = {tA, tB}
  Clients = {c1, c2}
  MaxEpoch = 2
  MaxCrashes = 2
  LegAdmissionGate = TRUE
  DeadmanGate = TRUE
  DeadmanCertain = TRUE
  RecordAssemblyOnly = TRUE
  ElectInSync = TRUE
  DegradeBarrier = TRUE
  FreeWaitsActive = TRUE
  FenceReplayOnAssemble = TRUE
  DeliveredEpochKeyed = FALSE
  RejoinEnabled = TRUE
  RecordRejoinOnly = TRUE
  UncleanResync = TRUE
  AncestryGuard = TRUE
  PreemptFollowsRecord = TRUE
  MaxCuts = 0
  MarkAtWitness = TRUE
  FenceAtWitness = TRUE
PROPERTIES
  ClientEventuallyRedirected
CHECK_DEADLOCK FALSE
