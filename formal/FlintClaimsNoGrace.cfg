\* Mutation run: the pre-F50 world — no marker grace (MarkerGrace =
\* FALSE), with the P1 Lease still ON.  TLC MUST find an
\* Inv_NoColdAdmission violation: a renewal hiccup deposes a LIVE leader
\* whose in-flight catch-up dispatch keeps running (the Lease gates only
\* tick starts); the new leader opens a window (young marker, prestaged
\* payload); the deposed leader's dispatch reads stale+marker — exactly
\* what a live pre-flip window looks like — and scrubs the payload; the
\* blind flip then commits a COLD leg into the serving raid.  F50's
\* E_f-scrub collision, rediscovered as a counterexample WITH the lease
\* on — machine-checked proof the grace and the Lease are complementary
\* layers, not redundant ones.  scripts/check-tla.sh requires this run
\* to FAIL.
SPECIFICATION Spec
CONSTANTS
  Procs = {p1, p2}
  MaxCrashes = 1
  LeaderGate = TRUE
  MarkerGrace = FALSE
INVARIANTS
  Inv_NoColdAdmission
CHECK_DEADLOCK FALSE
