\* THE SHIPPED AVAILABILITY ENVELOPE, strict (2026-07-29 audit): both
\* automatic arms ON — the gate's 180s defer deadline (GateDeadline:
\* serve-with-risk on transient evidence once the persisted deadline
\* passes) and the 2-base-floor forced-stale admission (StaleFloor: a
\* record-Stale leg auto-admitted, serving reads, evented only).  Deep
\* 2-leg budget so stale-only survival, torn writes and the deadline
\* interleavings are all reachable.  Must HOLD: InvCore — everything
\* EXCEPT Inv_NoFalseRisk, which is a theorem only of the GateDeadline =
\* FALSE idealization (the GateRealHollow run proves the arm really
\* trades it away) — with the per-leg forced-stale exemptions carrying
\* the mixed-assembly states; plus the post-storm liveness and
\* AdmissionNotStarved.  scripts/check-tla.sh requires success.
SPECIFICATION Spec
CONSTANTS
  Legs = {l1, l2}
  MaxWrites = 1
  MaxCrashes = 3
  GateStrict = TRUE
  RejoinGuard = TRUE
  FenceZombie = TRUE
  ClaimArb = TRUE
  EvidenceStrict = TRUE
  MaintEnabled = FALSE
  MaintFence = TRUE
  MaintBarrier = TRUE
  MaintLease = TRUE
  BarrierRaidAware = TRUE
  ExpandEnabled = FALSE
  SizeGuard = TRUE
  SizeHeal = TRUE
  GateDeadline = TRUE
  StaleFloor = TRUE
  MonitorCurrent = TRUE
  DeviceFloor = TRUE
  SuppressScoped = TRUE
  DrainBelt = TRUE
  RollerRace = FALSE
  RollerLeaderGate = TRUE
  DrainMarksBelt = TRUE
INVARIANTS
  InvCore
PROPERTIES
  EventuallyServingAgain
  EventuallyWritable
  AdmissionNotStarved
CHECK_DEADLOCK FALSE
CONSTRAINT
  GenBound
