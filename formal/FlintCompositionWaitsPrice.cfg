\* THE PRICE OF ElectInSync, REQUIRED TO FAIL (the GateReal precedent:
\* name what a belt costs, then make TLC produce the bill).  Shipped
\* constants, full fairness — and PromotionCompletesUnconditional (the
\* progress promise WITHOUT the in-sync conjunct) must be VIOLATED: a
\* volume serving degraded (peer leg duly marked stale) whose composer
\* then partitions is DOWN until that composer recovers.  The election
\* gate refuses the stale survivor — correctly, its leg is missing acked
\* bytes — and the suspended composer can drive no rebuild, so nothing
\* ever becomes electable.  Availability spent on durability, as a
\* lasso.  The undesigned escape is the operator override
\* (FlintReplication's LastResortServe analog: serve the freshest stale
\* survivor, risk surfaced), owed to the operator-surface work alongside
\* release_quarantine.
SPECIFICATION SpecLive
CONSTANTS
  Tgts = {tA, tB}
  Clients = {c1, c2}
  MaxEpoch = 2
  MaxCrashes = 2
  LegAdmissionGate = TRUE
  DeadmanGate = TRUE
  DeadmanCertain = TRUE
  RecordAssemblyOnly = TRUE
  ElectInSync = TRUE
  DegradeBarrier = TRUE
  FreeWaitsActive = TRUE
  FenceReplayOnAssemble = TRUE
  DeliveredEpochKeyed = FALSE
  RejoinEnabled = TRUE
  RecordRejoinOnly = TRUE
  UncleanResync = TRUE
  AncestryGuard = TRUE
  PreemptFollowsRecord = TRUE
PROPERTIES
  PromotionCompletesUnconditional
CHECK_DEADLOCK FALSE
