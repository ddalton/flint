\* F62 MUTATION — and the most uncomfortable run in this module, because the
\* world it mutates is not the SHIPPED code but the code as FIXED by F61.
\*
\* Identical to FlintReplicationRollProcessed.cfg (which is GREEN and which
\* blessed the F61 fix) except RaidLifetimeArm = TRUE: the raid composition
\* is now an object with its own lifetime instead of a fact derived from leg
\* health.  That single change turns the same behavior graph from green into
\* an outage, because the roll the F61 fix enabled deletes the csi-node pod
\* on the node HOSTING the composition, and the composition dies with the
\* tgt process.  Zero real failures (MaxCrashes = 0), both legs healthy and
\* on disk, both recorded in_sync — and the volume is gone for good.
\*
\* So this run is the model finally able to state the live finding from runao
\* 2026-07-30: F61's bug was LOAD-BEARING.  The livelock was the only thing
\* preventing the un-implemented local half from being exercised, and fixing
\* F61 alone converts a silent stall into a silent outage.  MaintLocalRefuse
\* = FALSE is exactly that intermediate state, and it must not be green.
\*
\* EXPECTED: Inv_PlannedRollNeverCausesOutage VIOLATED.
\* scripts/check-tla.sh runs this and requires the violation.
SPECIFICATION Spec
CONSTANTS
  Legs = {l1, l2}
  MaxWrites = 1
  MaxCrashes = 0
  GateStrict = TRUE
  RejoinGuard = TRUE
  FenceZombie = TRUE
  ClaimArb = TRUE
  EvidenceStrict = TRUE
  MaintEnabled = TRUE
  LocalLegs = {l2}
  MaintProcessedGate = TRUE
  RefusalSticky = FALSE
  ConsumerMobile = FALSE
  RaidLifetimeArm = TRUE
  MaintLocalRefuse = FALSE
  RaidReconcileArm = FALSE
  VaCanLag = FALSE
  NodeStageValidatesBases = FALSE
  StrikeRepairArm = FALSE
  A2LocalStagingBelt = FALSE
  A2SoleOwnershipBelt = FALSE
  UncontrolledTgtDeath = FALSE
  DpSeenRehydrate = FALSE
  MaintFence = TRUE
  MaintBarrier = TRUE
  MaintLease = TRUE
  BarrierRaidAware = TRUE
  ExpandEnabled = FALSE
  SizeGuard = TRUE
  SizeHeal = TRUE
  GateDeadline = FALSE
  StaleFloor = FALSE
  MonitorCurrent = TRUE
  DeviceFloor = TRUE
  SuppressScoped = FALSE
  DrainBelt = TRUE
  RollerRace = FALSE
  RollerLeaderGate = TRUE
  DrainMarksBelt = TRUE
  BounceEnabled = FALSE
  MaxBounces = 0
  AdmissionArm = FALSE
  DataPathArm = FALSE
  BouncePreflight = TRUE
  BounceRace = FALSE
  BounceLeaderGate = TRUE
  StageAdmit = FALSE
  PodLayer = FALSE
  ReconcilerBelt = TRUE
  DetachWaitHonored = TRUE
  PlannerDisjoint = FALSE
  WriterLimbo = FALSE
  RefusalBounded = FALSE
INVARIANTS
  Inv
  Inv_RaidCompositionCoupled
  Inv_PlannedRollNeverCausesOutage
CHECK_DEADLOCK FALSE
CONSTRAINT
  GenBound
