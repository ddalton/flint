\* Strict breadth: the FULL amended design — mutation guard, complete
\* fence inventory, disown evidence, rebind-rearm, holder-evidence
\* persistence, ladder recheck all ON.  Every invariant must HOLD across
\* two grants, two mutations, one channel death and one same-PVC
\* restart.  Liveness lives in FlintDelegRecallLive.cfg (invariants at
\* breadth, liveness at depth — the placement lesson).
SPECIFICATION Spec
CONSTANTS
  MaxMut = 2
  MaxGrants = 2
  MaxRestarts = 1
  MaxDies = 1
  MutationGuard = TRUE
  FenceComplete = TRUE
  DisownEvidence = TRUE
  RebindRearm = TRUE
  PersistHolderEvidence = TRUE
  LadderRecheck = TRUE
INVARIANTS
  Inv
CHECK_DEADLOCK FALSE
