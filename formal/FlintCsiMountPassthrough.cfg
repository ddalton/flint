\* WHY THE BUG WAS LEAN-ONLY.  Same blind oracle, guard off, but
\* SameFsBind = FALSE: the source is a foreign-filesystem FUSE mount
\* (the passthrough arm), which the device-number test DOES see.  This
\* run must HOLD.  It is the machine-checked form of the sentence that
\* explains months of green passthrough drills over a live defect — the
\* abstraction the bug hid in was "a mount is a mount".
SPECIFICATION Spec
CONSTANTS
  Clusters = {c1, c2}
  Files = {f1, f2}
  MountOracle = "dev"
  SameFsBind = FALSE
  CleanupGuard = FALSE
  LeaseCheck = TRUE
  DrainBeforeRelease = TRUE
  LeaseExpiry = TRUE
  MaxWrites = 1
  MaxCycles = 1
  MaxCrashes = 0
INVARIANTS
  TypeOK
  Inv_NoAckedLoss
  Inv_NoTreeLoss
  Inv_SingleWriter
CHECK_DEADLOCK FALSE
