\* THE SCOPE CORRECTION — the run that moved the fence IDENTITIES into
\* the witness.  The arbiter decision's first scoping said "client
\* fencing stays sqlite-local", reading the invariant (fencing must not
\* depend on the API server) as covering the fence RECORDS.  Single flag
\* off the Witness strict: FenceAtWitness = FALSE, fence rows live only
\* in the volume's home shard.  The fence replay belt is ON, fail-closed,
\* and passing its unit tests — and a SURVIVOR's assembly replays from
\* the only table it can read, its own, which is empty.  The fenced
\* victim re-attaches to the new export unfenced and writes a range that
\* was freed and re-granted a whole epoch ago.  TLC must FIND
\* Inv_NoStaleExtentWrite violated — the NoReplay counterexample arriving
\* through fact LOCALITY with the belt armed.  What stays sqlite-local is
\* the fence ENFORCEMENT lane (preempts, delivered marks, the sweep):
\* those actions carry no witness guard in the module, and the Witness
\* strict's green is the proof that lane never needed one.
SPECIFICATION Spec
CONSTANTS
  Tgts = {tA, tB}
  Clients = {c1, c2}
  MaxEpoch = 2
  MaxCrashes = 2
  LegAdmissionGate = TRUE
  DeadmanGate = TRUE
  DeadmanCertain = TRUE
  RecordAssemblyOnly = TRUE
  ElectInSync = TRUE
  DegradeBarrier = TRUE
  FreeWaitsActive = TRUE
  FenceReplayOnAssemble = TRUE
  DeliveredEpochKeyed = FALSE
  RejoinEnabled = TRUE
  RecordRejoinOnly = TRUE
  UncleanResync = TRUE
  AncestryGuard = TRUE
  PreemptFollowsRecord = TRUE
  MaxCuts = 2
  MarkAtWitness = TRUE
  FenceAtWitness = FALSE
INVARIANTS
  Inv_NoStaleExtentWrite
CHECK_DEADLOCK FALSE
