\* The shipped copy protocol: full chain walk, oldest-first order,
\* blobstore relink-on-delete. Every completed session must deliver
\* exactly the cut (Inv_SessionFaithful). scripts/check-tla.sh requires
\* success.
SPECIFICATION Spec
CONSTANTS
  Blocks = {b1, b2}
  MaxVer = 3
  MaxEpochs = 3
  MaxCuts = 3
  WalkFull = TRUE
  OrderedWalk = TRUE
  RelinkOnDelete = TRUE
INVARIANTS
  Inv
CHECK_DEADLOCK FALSE
