\* THE TWO-ROLLER RACE, gate ON (2026-07-29 — the tranche's headline
\* finding, F59 candidate): RollerRace with RollerLeaderGate = TRUE and
\* the shipped mutator (DrainMarksBelt = FALSE).  TLC must FIND
\* Inv_PlannedRollBoundedImpact violated at 3 legs with ZERO failures
\* and the lease fully honored: the roller passes is_leader at tick top
\* and captures a valid drain plan; leadership moves (the one budgeted
\* deposal overlap — not a crash); the new leader drains l1; the
\* deposed roller's in-flight commit then lands on l2 — the fresh-record
\* retry re-runs drain_for_maintenance, whose guards never look at other
\* legs' marks, and the ground-truth probe only refuses the LAST
\* configured base.  Two legs out of service under planned maintenance
\* alone.  The lease CANNOT close this: it is checked before the work
\* (one in-process atomic read per tick) while a tick's RPC work is
\* unbounded (300s HTTP timeouts × N volumes vs a 15s lease).
\* scripts/check-tla.sh requires the violation.
SPECIFICATION Spec
CONSTANTS
  Legs = {l1, l2, l3}
  MaxWrites = 1
  MaxCrashes = 0
  GateStrict = TRUE
  RejoinGuard = TRUE
  FenceZombie = TRUE
  ClaimArb = TRUE
  EvidenceStrict = TRUE
  MaintEnabled = TRUE
  LocalLegs = {}
  MaintProcessedGate = TRUE
  MaintFence = TRUE
  MaintBarrier = TRUE
  MaintLease = TRUE
  BarrierRaidAware = TRUE
  ExpandEnabled = FALSE
  SizeGuard = TRUE
  SizeHeal = TRUE
  GateDeadline = FALSE
  StaleFloor = FALSE
  MonitorCurrent = TRUE
  DeviceFloor = TRUE
  SuppressScoped = FALSE
  DrainBelt = TRUE
  RollerRace = TRUE
  RollerLeaderGate = TRUE
  DrainMarksBelt = FALSE
  BounceEnabled = FALSE
  MaxBounces = 0
  AdmissionArm = FALSE
  DataPathArm = FALSE
  BouncePreflight = TRUE
  BounceRace = FALSE
  BounceLeaderGate = TRUE
  StageAdmit = FALSE
  PodLayer = FALSE
  ReconcilerBelt = TRUE
  DetachWaitHonored = TRUE
  PlannerDisjoint = FALSE
  WriterLimbo = FALSE
  RefusalBounded = FALSE
INVARIANTS
  Inv_PlannedRollBoundedImpact
CHECK_DEADLOCK FALSE
CONSTRAINT
  GenBound
