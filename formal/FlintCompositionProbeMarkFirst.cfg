\* Tranche-4 non-vacuity probe, the second half of the serialization
\* pair: the MARK-FIRST order of the symmetric-partition race really
\* occurs — under a pure peerCut the composer lands its peer's stale
\* mark at the witness while the seat still reads epoch 1, and from that
\* state the peer's PromoteCAS is refused by ElectInSync in good faith.
\* ProbeCutFailover witnesses the CAS-first order; only the two together
\* license the Witness strict's claim to have explored both sides of the
\* race.  TLC must VIOLATE ProbeMarkFirst.
SPECIFICATION Spec
CONSTANTS
  Tgts = {tA, tB}
  Clients = {c1, c2}
  MaxEpoch = 2
  MaxCrashes = 0
  LegAdmissionGate = TRUE
  DeadmanGate = TRUE
  DeadmanCertain = TRUE
  RecordAssemblyOnly = TRUE
  ElectInSync = TRUE
  DegradeBarrier = TRUE
  FreeWaitsActive = TRUE
  FenceReplayOnAssemble = TRUE
  DeliveredEpochKeyed = FALSE
  RejoinEnabled = FALSE
  RecordRejoinOnly = TRUE
  UncleanResync = TRUE
  AncestryGuard = TRUE
  PreemptFollowsRecord = TRUE
  MaxCuts = 1
  MarkAtWitness = TRUE
  FenceAtWitness = TRUE
INVARIANTS
  ProbeMarkFirst
CHECK_DEADLOCK FALSE
