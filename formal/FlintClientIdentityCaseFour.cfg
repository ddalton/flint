\* MUTATION: case 4 replaces an unconfirmed record and starts the new
\* one at pending_replaces = None, dropping the obligation case 5 took
\* on one connection earlier. The shipped code before the fix. TLC must
\* find a mount that completes while the incumbent it superseded is
\* still in the client table.
SPECIFICATION Spec
CONSTANTS
  Agents = {a1, a2}
  Collide = TRUE
  NConnect = 2
  CarryObligation = FALSE
  CondIndexRemove = TRUE
  CascadeLocks = TRUE
  ModelLease = FALSE
  AtomicSweep = TRUE
  NotifyRevoked = FALSE
  MaxMounts = 2
  MaxLocks = 2
INVARIANTS
  Inv_NoOrphanLocks
  Inv_LocksReapable
  Inv_IndexCoversLiveOwners
  Inv_OneConfirmedPerOwner
  Inv_ObligationHonoured
CHECK_DEADLOCK FALSE
