apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: expandable-pvc
spec:
  resources:
    requests:
      storage: 2Gi
status:
  phase: Bound
  # Volume expansion requested but filesystem resize is pending
  conditions:
    - type: FileSystemResizePending
      status: "True"
