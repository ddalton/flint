apiVersion: v1
kind: Pod
metadata:
  name: reader-pod
status:
  phase: Succeeded
  # Optionally verify container exit code
  containerStatuses:
    - name: reader
      state:
        terminated:
          exitCode: 0
