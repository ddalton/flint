apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rwx-test-pvc
spec:
  accessModes:
    - ReadWriteMany  # Request RWX
  resources:
    requests:
      storage: 1Gi
  storageClassName: flint-nfs

