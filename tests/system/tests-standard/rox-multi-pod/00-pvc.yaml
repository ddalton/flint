apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc
spec:
  accessModes:
    - ReadWriteOnce
    - ReadOnlyMany  # Declare support for both RWO and ROX
  storageClassName: flint
  resources:
    requests:
      storage: 1Gi
