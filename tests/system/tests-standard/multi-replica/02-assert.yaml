apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 180
commands:
  - script: |
      # Wait for pod to complete
      echo "Waiting for pod to complete..."
      kubectl wait --for=jsonpath='{.status.phase}'=Succeeded pod/test-multi-replica-pod --timeout=120s
      
      # Verify pod completed successfully
      POD_STATUS=$(kubectl get pod test-multi-replica-pod -o jsonpath='{.status.phase}')
      if [ "$POD_STATUS" = "Succeeded" ]; then
        echo "✅ Pod completed successfully"
      else
        echo "❌ Pod status: $POD_STATUS"
        exit 1
      fi
      
      # Check the logs to verify data was written
      echo "Checking pod logs..."
      kubectl logs test-multi-replica-pod | tail -20
      
      if kubectl logs test-multi-replica-pod | grep -q "Data written successfully"; then
        echo "✅ Data written successfully to multi-replica volume"
      else
        echo "❌ Data write verification failed"
        exit 1
      fi
      
      # Get the node where pod ran
      NODE=$(kubectl get pod test-multi-replica-pod -o jsonpath='{.spec.nodeName}')
      echo "✅ Pod ran on node: $NODE"
      
      # Verify volume has 2 replicas
      PV_NAME=$(kubectl get pvc test-multi-replica-pvc -o jsonpath='{.spec.volumeName}')
      REPLICA_COUNT=$(kubectl get pv $PV_NAME -o jsonpath='{.spec.csi.volumeAttributes.flint\.csi\.storage\.io/replica-count}')
      if [ "$REPLICA_COUNT" = "2" ]; then
        echo "✅ Volume configured with 2 replicas"
      else
        echo "⚠️ Replica count: $REPLICA_COUNT (expected 2)"
      fi
      exit 0

