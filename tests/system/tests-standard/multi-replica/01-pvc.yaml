apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-multi-replica-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: flint-multi-replica

